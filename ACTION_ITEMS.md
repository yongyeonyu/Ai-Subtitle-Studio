# AI Subtitle Studio — 전체 액션아이템
# 최종 수정: 2026-04-23
# App Version: 02.02.00

================================================================================
  PHASE1-B 잔여
================================================================================

[인프라]
  R3   | AI 모델 관리 시스템                    | Windows 환경
       | - model_registry.json + install/uninstall UI
       | - OS별 모델 필터링 + 첫 실행 자동 체크

  R4   | Windows 전기능 연동                    | Windows 환경
       | - faster-whisper CUDA 연동
       | - PyQt6 DLL 호환
       | - 파일 경로 호환 (한글/공백)

  R8   | NAS 자동시작 기능                      | 공통
       | - iCloud와 동일한 드롭존 감시 방식으로 NAS 추가
       | - 현재 iCloud 자동시작만 존재

  R9   | 자동시작 체크 UI                       | 공통
       | - 메인 위젯에 iCloud/NAS 개별 ON/OFF 체크박스
       | - 체크 해제 시 자동 시작 비활성

  R10  | 자동시작 우선순위 로직                 | 공통
       | - iCloud + NAS 둘 다 ON → iCloud 우선 처리 후 NAS
       | - PHASE2에서 전체 파이프라인까지 연결됨


================================================================================
  PHASE2 — AI 러프컷 편집
================================================================================

── UI / 기반 ──

  P2-U0  | UI 레이아웃 설계
         | - 대표님이 그림으로 레이아웃 전달
         | - Copilot이 PyQt6 뼈대 UI 생성 → 확인 → 기능 추가

  P2-U1  | 전체화면 비디오 프리뷰
         | - 러프컷 최종안을 풀스크린으로 미리보기

── 프로젝트 / 데이터 ──

  P2-P1  | 프로젝트 파일 확장
         | - AI 러프컷 정보 저장: 컷 순서 변경/삭제/챕터/주제/summary

  P2-P2  | 챕터 자동 생성
         | - AI가 전체 자막을 챕터로 묶고 주제 부여

  P2-P3  | 챕터 기반 summary
         | - 챕터 순서에 따른 전체 영상 요약 자동 생성

  P2-P4  | 사용자 피드백 학습
         | - 러프컷 의견을 md 파일로 저장
         | - LLM이 스타일 지속 학습 → 러프컷 고도화

── AI 러프컷 핵심 ──

  P2-A1  | 멀티 AI 러프컷 생성
         | - 사용자가 선택한 여러 AI 모델이 각각 러프컷 후보 생성

  P2-A2  | 러프컷 후보군 비교/선택
         | - 후보군 나란히 보고 최종 결정

  P2-A3  | 러프컷 길이 설정
         | - AI 자동추천 (전체 내용 보고 판단)
         | - 사용자 목표 시간 설정: 목표 ± 오차
         | - 최소 단위 1분 (변경 가능하나 1분 미만 불가)
         | - 예: 10분 타겟 → 9~11분 사이로 생성

  P2-A4  | 컷 순서 변경/삭제
         | - 사용자가 AI 제안을 수동 조정 가능

── 출력 ──

  P2-O1  | 무손실 러프컷 출력
         | - 영상 품질 손실 없는 출력
         | - 멀티카메라 시 최고 품질 + 최고 fps 통일

  P2-O2  | 러프컷 자막 자동 재배치
         | - 컷 순서 변경 시 프로젝트 자막만 변경
         | - 개별 영상 자막 원본은 유지

  P2-O3  | 러프컷 SRT + 자막 영상 출력
         | - Phase1 출력 기능 확장

  P2-O4  | FCPXML 출력
         | - Final Cut Pro에서 최종 편집 가능하도록 XML 내보내기

── 자막 스타일 학습 ──

  P2-L1  | 교정 diff 자동 저장
         | - 자막백업 최신 bak (AI 원본) vs 사용자 수정 srt (최종)
         | - diff를 corrections.json에 자동 누적

  P2-L2  | "자막학습" 메뉴
         | - 에디터 위젯 내 메뉴 신설
         | - "학습" 버튼으로 프롬프트/md/LoRA 갱신 실행

  P2-L3  | 학습 실행 (Phase2)
         | - 교정 로그 → 최근 N개 프롬프트 주입
         | - 100개마다 LLM이 패턴 분석 → auto_style_guide.md 재생성

── 화자 인식 확장 ──

  P2-S1  | 멀티 화자 학습
         | - 화자 1/2/3 모두 개별 학습 (현재 화자1만)

  P2-S2  | 영상 구간 선택 학습
         | - 편집 중 영상에서 구간 선택 → 해당 구간 오디오 추출
         | - 화자별 학습 WAV로 output/data에 저장

  P2-S3  | 학습 소스 이중화
         | - 기존: WAV 파일 불러오기
         | - 추가: 현재 영상에서 구간 발췌

── 자동화 설정 ──

  P2-AUTO1 | "경로설정" → "자동화 설정" 리네이밍
           | - 메인 화면 메뉴 변경

  P2-AUTO2 | 자동화 파이프라인 설정 UI
           | - 각 단계 ON/OFF + 세부 옵션

  P2-AUTO3 | 원스톱 순차 실행
           | - STT → 자막 → 러프컷 → 출력 → 업로드 자동 진행

  P2-AUTO4 | 탭 기반 통합 설정 패널
           | - 하나의 화면: STT/자막/화자/러프컷/출력/업로드 탭
           | - 에디터 진입 없이 메인에서 모든 설정 수정 가능

  P2-AUTO5 | 에디터 설정과 동기화
           | - 자동화 설정 변경 = 에디터 설정과 동일 데이터 공유

  P2-AUTO6 | 자동시작 → 전체 파이프라인 연결
           | - 자동시작 시 STT→러프컷→업로드까지 자동 진행


================================================================================
  iPad 앱
================================================================================

  iPad-1 | 자막 에디터 UI
         | - SwiftUI 기반, 키보드 입력 최소화

  iPad-2 | 음성 STT 입력 모드
         | - 무음 세그먼트 생성 → 듣고 말해서 자막 작성

  iPad-3 | LLM 오탈자 자동 추천
         | - 입력 시 실시간 교정 추천

  iPad-4 | 멀티클립 기능
         | - Phase1 멀티클립 편집 기능 이식

  iPad-5 | AirDrop/iCloud 연동
         | - Phase1 산출물 전달 → 앱에서 바로 열기

  iPad-6 | 프로젝트 양방향 동기화
         | - iPad ↔ Mac/Windows
         | - 원본 영상 따로 + 프로젝트 따로 전송 가능

  iPad-7 | 유료 과금
         | - 무료: 자막 편집/타이밍 조정
         | - 유료: Whisper STT + LLM 최적화 + 자동 화자 분리


================================================================================
  PHASE3 — 자동 배포 + 고급 학습 + 원격 제어
================================================================================

  P3-C1   | 프록시 자동 업로드
          | - 러프컷 완료 → 480p/30fps 인코딩
          | - iCloud / Google Drive / YouTube 자동 업로드

  P3-L4   | LoRA 파인튜닝
          | - 교정 데이터 500개+ 누적 후
          | - 에디터 "자막학습" 메뉴에서 커스텀 모델 학습 실행

  P3-API1 | 앱 내장 REST API 서버
          | - FastAPI/Flask 경량 서버 (로컬 네트워크)
          | - Open-WebUI에서 호출 가능

  P3-API2 | UI 버튼 인덱싱
          | - 모든 버튼/액션에 고유 ID 부여
          | - API로 원격 트리거 가능

  P3-API3 | 파일/폴더 리스트 API
          | - 지정 경로의 파일 목록 조회 엔드포인트

  P3-API4 | 파이프라인 실행 API
          | - 자동화 설정 + 시작 명령 원격 호출

  P3-API5 | Open-WebUI 도구 연동
          | - Open-WebUI Function Calling으로 앱 API 호출
          | - 폰에서 대화형으로 파일 확인 → 자동화 실행


================================================================================
  완료 항목 (v02.02.00 기준)
================================================================================

  [버그 수정] B1~B12 전체 완료
  [기능]     F2/F3/F8/F9/F10/F12 완료
  [인프라]   R1(폴더구조5단계) R2(헤더통일) R5(맥LLM) R6(파일분할) R7(shim제거)
  [문서]     STRUCTURE.txt / AGENTS.md / RELEASE_v02.02.00.md


================================================================================
  추천 진행 순서
================================================================================

  PHASE1-B 마무리 (Windows)
    └→ R3 모델 관리 → R4 Windows 연동 → R8~R10 NAS 자동시작
    └→ v02.03.00 릴리즈

  PHASE2 시작
    └→ P2-U0 (UI 그림 → 뼈대)
    └→ P2-AUTO1~6 (자동화 설정)
    └→ P2-P1~P4 (프로젝트 확장)
    └→ P2-A1~A4 (AI 러프컷)
    └→ P2-L1~L3 (자막 학습)
    └→ P2-S1~S3 (화자 확장)
    └→ P2-O1~O4 (출력)
    └→ v03.00.00 릴리즈

  iPad 앱 (별도 프로젝트)
    └→ iPad-1~7 → App Store 출시

  PHASE3
    └→ P3-C1 (프록시 업로드)
    └→ P3-L4 (LoRA)
    └→ P3-API1~5 (Open-WebUI 연동)
    └→ v04.00.00 릴리즈

================================================================================
