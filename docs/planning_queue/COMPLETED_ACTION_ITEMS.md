<!--
Document-Version: 04.01.00-source-app
Phase: SOURCE_APP_CONTINUATION_V4_1_0
Last-Updated: 2026-06-28
Updated-By: Codex
Purpose: Completed action item archive separated from docs/planning_queue/ACTION_ITEMS.md.
-->
# COMPLETED_ACTION_ITEMS.md - Completed Action Item Archive

This file keeps completed action-item history out of `docs/planning_queue/ACTION_ITEMS.md`.
`docs/planning_queue/ACTION_ITEMS.md` remains the active execution queue and should contain only
remaining work, active gates, and rollback rules.

When an active item or sub-slice is completed, move its concise completion
summary here and remove that completed body from `docs/planning_queue/ACTION_ITEMS.md`. The active
queue may keep only a short archive pointer back to the relevant heading here.

Archive source labels use stable action-item titles or source sections instead
of active queue numbers, because the active queue order can change as completed
items are removed.

## Documentation Relocation And App Store Launch Planning

Source request: owner asked to create a Mac App Store launch plan, group and detail the active action items, move root development docs into docs, update/consolidate/retire docs, and update `AGENTS.md` for next-chat continuation.

1. Root development docs were moved under `docs/`; `AGENTS.md` remains the only root development-documentation file.
2. `docs/planning_queue/ACTION_ITEMS.md` now uses grouped active plans: `G0` Mac App Store launch, `G1` STT2/word precision latency, and `G2` source-app NLE/Taption continuity.
3. `docs/APP_STORE_SUBMISSION_READINESS.md` now contains a Phase 0-6 Mac App Store launch plan and readiness definition.
4. Docs hub, folder README files, project state, feature registry, validation guide, Jammini mapping, product README, file structure, code map, and release-note pointers were updated to the new docs layout.
5. Path-dependent scripts/tests were updated: Jammini watchdog/delegate/bootstrap, subtitle domain-map test, help QA ownership, and NAS truth-learning default plan path.
6. `docs/quality_validation/NAS_SUBTITLE_BENCHMARK_50_PLAN.md` and `docs/quality_validation/NAS_SUBTITLE_BENCHMARK_RECORDING_CONTEXT.md` now own the NAS benchmark docs instead of leaving them in docs root.
7. No runtime subtitle-generation policy, UI/UX, package build, upload, signing, notarization, DMG, App Store submission, STT/default-cache promotion, or persisted NLE disk-format change was performed.

## v04.01.00 Source-App Release

Source request: owner asked to run code review/fixes, clean the worktree, commit/push main, proceed with release `04.01.00`, and update development docs.

1. `core/runtime/config.py` was bumped to `APP_VERSION = "04.01.00"`.
2. `core/project/project_format.py` was bumped to project schema version `04.01.00`.
3. `tests/test_trace_logger.py` was fixed to assert trace manifest app version against `config.APP_VERSION` instead of a release-specific literal.
4. `docs/release_notes/RELEASE_v04.01.00.md` was added as the source-app release checkpoint note.
5. `docs/project_reference/PRODUCT_README.md`, `AGENTS.md`, `docs/planning_queue/ACTION_ITEMS.md`, `docs/nle_engine/NLE_Action.md`, `docs/README.md`, `docs/PROJECT_STATE.md`, and `docs/APP_STORE_SUBMISSION_READINESS.md` were synced to the new checkpoint.
6. Jammini plus 한결/서린/유진 release reviews were collected through physical handoff files and classified by Dex.
7. App Store readiness remains blocked and was not promoted to submission-ready; DMG, packaging, notarization, upload, persisted NLE disk fields, per-pixel NLE writes, UI/QML defaults, and STT/default-cache promotion were not performed.

## Development Documentation Organization And Active Queue Hygiene

Source request: owner asked to delete completed action items from `docs/planning_queue/ACTION_ITEMS.md` and organize development docs by referencing Taption docs/AGENTS rules.

1. `docs/planning_queue/ACTION_ITEMS.md` was tightened back to active gates: completed NLE regression proof bullets were removed from the active STT2 item and the migration status section now keeps only current source-app/native-migration guardrails.
2. Completed proof remains discoverable through `docs/planning_queue/COMPLETED_ACTION_ITEMS.md`, `docs/quality_validation/test_result.md`, `docs/HANDOFF.md`, and `output/manual_verification/latest/` instead of being duplicated in the active queue.
3. `docs/README.md` was rewritten as the development-documentation hub, with Taption-style role folders adapted to AI Subtitle Studio.
4. Role README files were added under `docs/planning_queue/`, `docs/workflow_operations/`, `docs/project_reference/`, `docs/quality_validation/`, `docs/product_behavior/`, `docs/nle_engine/`, `docs/speech_stt/`, `docs/validation_evidence/`, `docs/release_notes/`, and `docs/archive_legacy/`.
5. `AGENTS.md` now documents development-documentation organization rules, root canonical document preservation, active-only queue policy, physical Jammini handoff priority, and clean-room Taption reference handling.
6. Runtime code, UI/UX, STT/default-cache policy, App Store packaging/signing/upload, and persisted NLE disk format were not changed.

## STT2 / Word Precision Generation Latency Profiling And Accuracy-Preserving Trim

Source item: `ACTION_ITEMS.md` item `STT2 / Word Precision Generation Latency Profiling And Accuracy-Preserving Trim`.

1. Verifier repeat artifacts now expose STT2/word counts, final stability gates, global canvas stability, memory pressure, and generation-owner profile summaries.
2. Same-fixture HeyDealer 180s baseline/profile/reference evidence was captured under `output/manual_verification/latest/stt2_word_precision_latency_20260627/`.
3. True wall-clock stage spans now cover STT1 primary transcription, selective STT2 rescue, word timestamp precision, VAD/STT consensus, and subtitle postprocess in both verifier and reference-scored benchmark artifacts.
4. First redundant LLM preparation candidate was implemented by deferring local model resolution/warmup until macro gate rows actually require LLM.
5. Post-trim verification used the stricter method: unit no-call guard, non-profile repeat, profiler diagnostic, and reference-scored fixture with quality/timing/final-stability metrics.
6. The High context-boundary LLM batching candidate was rejected and reverted because stricter reference scoring detected small quality/text/segmentation drift despite lower postprocess time.
7. Substage timing was added for STT2/word precision prepare/collect/annotate/batch phases so the next trim candidate can target the true substage instead of the broad stage bucket.
8. Collect fallback precision instrumentation was added so the next long-fixture run can separate true STT2/word precision collect work from WhisperKit empty/timeout fallback overhead.
9. High context-boundary pair diagnostics were added so optimization candidates can distinguish elapsed time from candidate count, LLM call count, failed calls, and actual changed-pair count.
10. `tools/verify_reference_fixture_availability.py` and focused tests were added so reference media/SRT readiness is checked before any latency trim can be treated as quality-accepted.
11. A local X5 60s reference-scored smoke fixture was restored from cached JSON rows and verified with `mode_high`; this remains short-loop evidence only.
12. A longer X5 180s project-reference smoke fixture was restored, and `tools/evaluate_reference_benchmark_acceptance.py` was added so semantically mismatched media/SRT pairs are rejected after scoring.
13. `/Volumes/photo` was mounted, the NAS HeyDealer MP4 plus matching SRT were verified, the owner-required 3-minute preflight/reference benchmark ran, and the scored result was accepted.
14. STT2/word precision range-audio, prepared-audio, and applied-segment diagnostics were added and verified on the same NAS HeyDealer 180s fixture with accepted reference scoring.
15. STT2/word precision reason-breakdown diagnostics were added and verified on the same NAS HeyDealer 180s fixture with accepted reference scoring.
16. High context-boundary decision-action diagnostics were added and verified on the same NAS HeyDealer 180s fixture with accepted reference scoring.
17. The strict High context-boundary keep/no-correction cache and benchmark `--setting` override surface were implemented; focused tests passed.
18. Benchmark CLI setting precedence was fixed so explicit `--setting` values win over mode-profile defaults; focused test covered the regression.
19. After the owner turned NAS off and requested generated video/subtitle validation, a 180.583s Korean synthetic fixture with 54 reference rows was created, two High-mode generation benchmarks ran, both scored results were accepted, High context keep-cache hits were proven to eliminate repeat LLM calls without changing final text/timing/overlap metrics, and a fresh NAS-off direct validation was rerun on 2026-06-28 with `accepted=true`, raw/final/reference `54/54/54`, and final invalid/non-monotonic/overlap `0/0/0`.
20. A macro proofread response replay cache was implemented. It stores exact prompt/model/provider response chunks and, on replay, still reruns candidate-lock/verifier/Deep rerank before preserving or rejecting the output; the owner-approved generated 3-minute fixture showed proofread postprocess `30.731199s -> 0.545337s` with identical scored quality/final gates and accepted reference results.
21. An opt-in STT2/word precision collect replay cache was implemented. It stores exact prepared-audio/model/settings collect output and, on replay, still reruns annotation, STT2 replacement selection, word precision timing application, final integrity, and reference acceptance; the owner-approved generated 3-minute fixture showed STT2 collect `14.284272s -> 0.0s`, word precision collect `10.930693s -> 0.0s`, elapsed `46.498s -> 20.105s`, and identical scored quality/final gates.
22. STT1 primary collect diagnostics were added and verified on the generated 3-minute fixture. STT1 primary transcribe is now split into setup/collect metadata; the latest run showed setup `0.046327s` and collect `19.986159s`, so no redundant setup/idle churn was accepted as an STT1 trim target.
23. An opt-in STT1 primary collect replay cache was implemented. It stores exact chunk-audio/model/settings collect output and, on replay, still runs STT2 selection, word precision, VAD/STT consensus, LLM/LoRA postprocess, final integrity, and reference acceptance; the owner-approved generated 3-minute fixture showed STT1 collect `17.717081s -> 0.0s`, elapsed `51.964s -> 37.715s`, and identical scored quality/final gates.
24. Exact replay cache keys were normalized so unrelated cache enable/path/max-entry controls do not cause duplicate STT1 or STT2/word provider work when multiple caches are enabled together; the owner-approved generated 3-minute fixture passed two combined-cache scored runs and produced a final SRT with invalid/non-monotonic/overlap `0/0/0`.
25. Runtime LLM model resolution and Ollama warmup are skipped when every LLM macro candidate group has an exact macro response cache hit; the owner-approved generated 3-minute fixture passed with elapsed `1.312s`, macro proofread `0.400186s`, identical scored quality/final gates, and generated SRT overlap `0`.
26. The owner-requested NAS-off generated-video validation was rerun on the current worktree. Benchmark `20260628_010403` accepted under the legacy score/overlap gate with raw/final/reference `54/54/54`, final/SRT invalid-nonmonotonic-overlap `0/0/0`, global max active `1`, and generated final SRT rows `54`.
27. Strict generated-video validation was promoted into the acceptance path. `tools/evaluate_reference_benchmark_acceptance.py` now probes media duration when possible and rejects final `last_end` beyond the media/window duration bound; generated benchmark summaries now expose final min/max segment duration plus short/long segment counts. Re-evaluating `.codex_work/benchmarks/subtitle_pipeline_variants/20260628_010403/benchmark_results.json` now returns `accepted=false` with reason `final_last_end_beyond_duration_bound`; artifact: `output/manual_verification/latest/generated_video_strict_acceptance_gate_20260628/reference_benchmark_acceptance.md`.
28. The generated-fixture tail collapse was fixed. `vad_stt_timing_consensus` now requires VAD/STT1 spans to be similar before the STT1/VAD-only union path can apply, preventing broad full-file VAD spans from stretching later STT1 rows. Re-run `20260628_013224` passes strict acceptance with final last end `180.12s`, short/long counts `0/0`, and no overlap.
29. The prepared recheck clip metadata reuse candidate was rejected and reverted because it did not materially reduce prepare time and added directory/metadata state complexity.
30. NAS-off stage/memory variance analysis tooling and evidence were added. `tools/summarize_stage_variance.py` generated `output/manual_verification/latest/stt_latency_stage_variance_20260628/stage_variance_summary.md` from existing cache/generated benchmark artifacts and confirmed this is analysis-only, not a production speed approval.
31. STT collect-cache backfill readiness audit tooling and evidence were added. `tools/audit_stt_cache_backfill_readiness.py` generated `output/manual_verification/latest/stt_cache_backfill_readiness_20260628/stt_cache_backfill_readiness.md` from existing benchmark artifacts and confirmed collect-cache defaults remain `false/false`, production recommendation is `hold_default_off`, current real inputs are unavailable, strict real-media cache-hit replay count is `0`, and old generated cache-hit artifacts must be refreshed because they fail the strict duration-bound final gate.
32. The tail-collapse-fixed strict synthetic collect-cache write/hit replay was completed and moved out of the active queue. Write run `20260628_081537` and hit run `20260628_081711` both passed strict reference acceptance on the generated 180s fixture; the hit run completed in `1.131s`, kept raw/final/reference `54/54/54`, quality/text/timing `93.411/91.676/0.1391s`, final invalid/non-monotonic/overlap `0/0/0`, final last end/duration bound `180.12/180.584`, global max active `1`, STT1/STT2/word provider calls `false`, and macro provider group `0`. The refreshed readiness audit now reports strict generated cache-hit runs `1` but still blocks production default promotion with `hold_real_media_backfill_required`.
33. STT collect-cache backfill readiness was tightened so representative real-media evidence requires both a strict cache-write run and a strict cache-hit replay before owner review. `tools/audit_stt_cache_backfill_readiness.py` now emits a next-run command plan, forbidden-substitute list, and owner-review gate; evidence: `output/manual_verification/latest/stt_cache_backfill_command_plan_20260628/stt_cache_backfill_readiness.md`. The current audit still reports collect-cache defaults `false/false`, current real inputs unavailable, strict real-media cache-write/cache-hit counts `0/0`, and production recommendation `hold_default_off`.
34. The representative HeyDealer first-180s real-media collect-cache write/hit backfill was strict-accepted after narrowing benchmark hypothesis rows to the requested window. `tools/benchmark_subtitle_pipeline_variants.py` now records benchmark-window projection diagnostics; write run `20260628_105017` and hit run `20260628_105119` both passed `evaluate_reference_benchmark_acceptance.py` with elapsed `46.073s -> 1.266s`, raw/final/reference `58/56/89`, quality/text/timing `93.766/94.267/0.5808s`, final invalid/non-monotonic/overlap `0/0/0`, final last end/duration bound `180.0/180.0`, short/long `0/0`, global max active `1`, and STT1/STT2/word provider calls `true -> false`. Evidence: `output/manual_verification/latest/stt_cache_tail_bound_fix_20260628_1048/`. Production collect-cache defaults remain `false/false` until explicit owner review.
35. After the owner reported the NAS was back online, the current HeyDealer first-180s MP4/SRT preflight was refreshed and passed at `output/manual_verification/latest/heydealer_nas_preflight_current_20260628/reference_fixture_availability.md`; this confirms current inputs are reachable but does not approve collect-cache default promotion without explicit owner review.
36. STT worker timeout artifact audit tooling was added. `tools/audit_stt_worker_timeout.py` reads benchmark results, detects `stt_collect_whisperkit_fallback` timeout spans and timeout-like word-precision collect failures, compares timeout and non-timeout NAS runs, and keeps blocked runtime changes explicit. Evidence: `output/manual_verification/latest/stt_worker_timeout_compare_20260628/stt_worker_timeout_audit.md`; baseline run `20260628_113906` had timeout elapsed `0s` and elapsed `45.491s`, while run `20260628_123336` had timeout elapsed `330.132245s`, timeout ratio `0.88198`, final invalid/non-monotonic/overlap `0/0/0`, global max active `1`, and quality/text/timing `93.955/94.867/0.5536s`. This is read-only diagnostic evidence only; model downgrade, STT2/word precision skipping, quality-gate relaxation, collect-cache default promotion, UI changes, and App Store work remain blocked.
37. After the owner reported NAS was on again, the live HeyDealer first-180s MP4/SRT preflight and High-mode regression were refreshed. Preflight passed at `output/manual_verification/latest/heydealer_nas_preflight_live_20260628/reference_fixture_availability.md`; benchmark run `.codex_work/benchmarks/subtitle_pipeline_variants/20260628_141640/benchmark_results.json` passed strict acceptance at `output/manual_verification/latest/stt_nas_live_heydealer_20260628/acceptance/reference_benchmark_acceptance.md` with elapsed `45.631s`, raw/final/reference `58/56/89`, quality/text/timing `93.766/94.267/0.5808s`, final invalid/non-monotonic/overlap `0/0/0`, final last end/duration bound `180.0/180.0`, short/long `0/0`, global max active `1`, and STT1/STT2/word selected `21/37/7`. `output/manual_verification/latest/stt_worker_timeout_compare_nas_live_20260628/stt_worker_timeout_audit.md` reports `timeout_detected=false`; collect-cache defaults remain `false/false` pending explicit owner review.
38. After the owner connected NAS at `192.168.0.5`, the current `/Volumes/photo` HeyDealer first-180s MP4/SRT collect-cache backfill was refreshed. Preflight passed at `output/manual_verification/latest/stt_cache_backfill_real_nas_20260628_2202/preflight/reference_fixture_availability.md` with the real MP4/SRT present and clipped reference rows `89`. Write run `.codex_work/benchmarks/subtitle_pipeline_variants/20260628_220327/benchmark_results.json` and hit run `.codex_work/benchmarks/subtitle_pipeline_variants/20260628_220718/benchmark_results.json` both passed strict acceptance at `output/manual_verification/latest/stt_cache_backfill_real_nas_20260628_2202/acceptance_write/reference_benchmark_acceptance.md` and `output/manual_verification/latest/stt_cache_backfill_real_nas_20260628_2202/acceptance_hit/reference_benchmark_acceptance.md` with elapsed `177.888s -> 1.183s`, raw/final/reference `58/56/89`, quality/text/timing `93.766/94.267/0.5808s`, final invalid/non-monotonic/overlap `0/0/0`, final last end/duration bound `180.0/180.0`, short/long `0/0`, global max active `1`, STT1/STT2/word collect provider calls `true -> false`, and cache-hit collect elapsed `0.0s`. Readiness refresh at `output/manual_verification/latest/stt_cache_backfill_real_nas_20260628_2202/readiness_refresh/stt_cache_backfill_readiness.md` reports `current_real_inputs_available=true`, strict real hits `2`, defaults `false/false`, and production recommendation `hold_default_off`. Timeout audit `output/manual_verification/latest/stt_cache_backfill_real_nas_20260628_2202/timeout_audit/stt_worker_timeout_audit.md` reports `timeout_detected=false`. This completes the refresh evidence slice only; collect-cache default promotion remains pending explicit owner review.

## Mac App Store Submission Readiness

Source item: `ACTION_ITEMS.md` item `Mac App Store Submission Readiness`.

1. `tools/audit_app_store_readiness.py`, `tests/test_app_store_readiness_audit.py`, and `docs/APP_STORE_SUBMISSION_READINESS.md` were added to keep App Store submission readiness evidence separate from source-app pytest/QA.
2. The submission target was locked to Mac App Store `.pkg`, and Developer ID beta `.dmg` was documented as a separate opt-in track that cannot count as App Store submission proof.
3. The readiness audit now itemizes non-code submission contents with per-item `status`, `draft`, `owner_decision_required`, and `acceptance_gate` fields for privacy policy URL, App Privacy answers, export compliance, screenshots, support URL, app review notes, age rating, and release notes. Evidence: `output/manual_verification/latest/app_store_submission_contents_audit_20260628/app_store_readiness_audit.md`; current result remains `app_store_submission_ready=false`, blocker count `14`, submission content status `blocked`, and pending owner-input items `8/8`.
4. After owner approval for App Store packaging/signing/upload/metadata execution, the local app bundle Python runtime and signing path were fixed. `packaging/macos/fix_bundled_python_runtime.py` makes the bundled Python runtime self-contained, `build_app_bundle.sh` applies it and sets bundle-local `PYTHONHOME`/`PYTHONPATH`, `validate_app_bundle.sh` rejects external or broken symlinks, and `sign_app_bundle.sh` signs nested code before the top-level app entitlements pass. Evidence: `output/manual_verification/latest/app_store_owner_approval_packaging_20260628_2220/` includes bundle build, symlink scans, internal `otool` paths, Apple Development signing smoke, strict codesign verify, final validation, and bundled Python smoke. Focused tests passed with `9 passed`. The refreshed readiness audit at `output/manual_verification/latest/app_store_owner_approval_readiness_after_packaging_fix_20260628_2250/app_store_readiness_audit.md` remains blocked with blocker count `13` because Apple Distribution and 3rd Party Mac Developer Installer identities, signed `.pkg`, sandbox workflow smoke, App Store Connect validation, and owner metadata values are still missing.

## App Command/Snapshot Acknowledgement Cleanup

Source section: previous `ACTION_ITEMS.md` parked candidate `App command/snapshot acknowledgement cleanup`.

1. A rollback branch was created before execution: `codex/rollback-app-command-ack-20260628-0918`.
2. `tools/appctl.py` now annotates direct `capture-snapshot` / `snapshot` CLI responses with `data.artifact` and `data.artifact_ready` so a queued/ok response cannot be mistaken for saved PNG proof without checking the file path and size.
3. `tools/appctl.py` now annotates `guided-subtitle-run` `command_timeout` responses with a safe follow-up `guided-subtitle-status` result plus `post_timeout_evidence`; the original timeout remains non-ok, but the CLI output can show whether the requested media path, processing state, backend activity, or guided snapshot run suggests work may have started.
4. Runtime bridge handlers, UI/UX behavior, subtitle generation, STT/STT2, NLE state, App Store readiness, packaging, signing, and upload behavior were not changed.
5. Jammini scout: `.agents/sentinel/handoffs/20260628-091900-app-command-ack-cleanup-scout.md`; Dex classified the slice as accepted with a narrower implementation scope than the scout's runtime-handler suggestion.
6. Verification: `./venv/bin/python -m py_compile tools/appctl.py tests/test_appctl.py` passed; `QT_QPA_PLATFORM=offscreen ./venv/bin/python -m pytest -q tests/test_appctl.py tests/test_automation_command_client.py tests/test_remote_verify_actions.py` -> `14 passed`; `QT_QPA_PLATFORM=offscreen ./venv/bin/python -m pytest -q tests/test_app_command_bridge.py -k "guided_subtitle_run or capture_snapshot or command_timeout"` -> `7 passed, 71 deselected`.

## Playhead-Only Dirty-Rect Repaint Candidate Gate

Source section: previous `ACTION_ITEMS.md` parked candidate `Playhead-only dirty-rect repaint`.

1. A rollback branch was created before execution: `codex/rollback-playhead-dirty-rect-gate-20260628-0925`.
2. The candidate was closed as `hold_full_canvas_repaint`, not implemented. Current `TimelineSingleOwnerPlayheadInvalidation` remains `qwidget-2d-full-canvas-repaint`, and `runtime_change_allowed=false` until fresh Macau visual smoke proves no residue and the owner approves a default change.
3. `tools/audit_editor_rendering_ownership.py` now writes JSON/Markdown evidence with a `playhead_dirty_rect_candidate` gate. Evidence: `output/manual_verification/latest/playhead_dirty_rect_gate_20260628/editor_rendering_ownership_audit.md`; result `ok=true`, issue count `0`, candidate status `hold_full_canvas_repaint`.
4. Runtime repaint behavior, UI/UX, timeline drawing, NLE state, subtitle generation, STT/STT2, App Store readiness, packaging, signing, and upload behavior were not changed.
5. Jammini scout: `.agents/sentinel/handoffs/20260628-092600-nle-playhead-dirty-rect-scout.md`; verdict recommends Defer/Reject for runtime dirty-rect optimization before Macau visual smoke.
6. Verification: `./venv/bin/python -m py_compile tools/audit_editor_rendering_ownership.py tests/test_editor_rendering_ownership_audit.py` passed; `QT_QPA_PLATFORM=offscreen ./venv/bin/python -m pytest -q tests/test_editor_rendering_ownership_audit.py tests/test_timeline_playhead_fit.py -k "single_owner_playhead_invalidation or playhead_canvas_repaints_full_2d_owner or shadow_playhead_repaints_canvas_full_2d_owner"` -> `3 passed, 194 deselected`; `./venv/bin/python tools/audit_editor_rendering_ownership.py --output-dir output/manual_verification/latest/playhead_dirty_rect_gate_20260628` wrote the evidence artifact.

## Trace Log Bundle Diagnostics

Source section: `NLE_Action.md` Trace Log Bundle.

1. Trace Log Bundle contract audit tooling was added. `tools/audit_trace_log_bundle.py` creates a bounded temporary trace run, verifies required temp directories, manifest/latest/events JSONL, exact-frame `fps_num`/`fps_den`, bounded media fingerprinting, trace package collection, and non-disabled trace status, then writes JSON/Markdown evidence.
2. Trace run-directory retention was added under `core/runtime/temp_workspace.py` and invoked by `core/runtime/trace_logger.py` before creating a new trace run. The current policy keeps at most 20 trace run directories after a new run starts.
3. Evidence: `output/manual_verification/latest/trace_log_bundle_retention_audit_20260628/trace_log_bundle_audit.md`; result `passed=true`, required dirs `true`, frame precision `true`, bounded media fingerprint `true`, package complete `true`, retention ok `true`, retained run count `20/20`, retention removed count `5`, trace disabled `false`, drop counts `{}`.
4. Trace package-directory retention was added under `core/runtime/temp_workspace.py` and invoked by `tools/collect_trace_package.py` after package collection. The current policy keeps at most 10 `Diagnostics/Packages/AISSTrace-*` directories, preserves the current package, records the retention report in `package_manifest.json`, and keeps trace/package cleanup bounded without changing runtime UI/STT/NLE behavior. Evidence: `output/manual_verification/latest/trace_package_retention_contract_20260628/trace_log_bundle_audit.md`; result `passed=true`, package retention ok `true`, retained package count `10/10`, package retention removed count `4`, trace run retention still `20/20`, package complete `true`, and trace disabled `false`.

## NLE_Action Completed Workstream Baseline

Source section: previous `NLE_Action.md` current-status completed list.

1. Domain contract and `NLESnapshot` baseline work completed.
2. Roughcut exact-join markers, render/export parity, and save/reopen compatibility guards completed.
3. Source-fps scout frame preservation completed.
4. Temporary trace workspace and Trace Log Bundle contract audit plus run/package retention completed.
5. Preview/skimming cache ownership completed.
6. The in-memory `NLEProjectState` editor/save owner pilot completed.
7. Main timeline canvas read/projection cutover completed.
8. Known safe commit-boundary mutable sync sources completed.
9. Provisional cut-boundary `marker_edit` completed.
10. Persistence cutover audit matrix for all 12 current NLE dual-write operation families completed.

## Source-App NLE Runtime Adoption And Migration Status

Source sections: previous `ACTION_ITEMS.md` migration status and completed slices from `NLE Timeline Canvas State Ownership: Commit-Boundary Mutable Sync`.

1. Owner-approved persisted NLE snapshot compatibility metadata was added. `core/project/project_format.py` can now write `nle_snapshot` only when a project explicitly carries `nle_persistence.persist_snapshot=true` with `approval=owner_approved_20260628`; `core/project/nle_persistence_guard.py` allows only that approved snapshot shape and still strips/quarantines unapproved top-level `nle`, arbitrary `nle_snapshot`, and persisted `_nle_project_state` payloads. Evidence: `output/manual_verification/latest/nle_approved_snapshot_persistence_20260628_2315/nle_persistence_cutover_audit.md`; `prep_ready=true`, approved snapshot persistence `ready=true`, legacy rows stable `true`, runtime NLE state hydrated `true`, operation roundtrip all passed across `11` families, render/export parity passed, and full cutover remains blocked by top-level `nle`, runtime-state persistence, and legacy-compatibility gates.
2. Approved persisted `nle_snapshot` read-back parity was added without making the snapshot the canonical load owner. `core/project/nle_snapshot.py` now builds a runtime `_nle_snapshot_readback_parity` report by comparing the persisted approved snapshot signature against a freshly rebuilt snapshot, and `core/project/project_io.py` attaches it after disk/cache reads. The report is stripped by `project_format` and `project_manager`, so drift evidence does not persist back to `.aissproj`. Evidence: `output/manual_verification/latest/nle_snapshot_readback_parity_20260628_2345/nle_persistence_cutover_audit.md`; approved snapshot read-back parity is stable with mismatch count `0`, intentionally corrupted snapshot drift is detected with mismatch count `1`, legacy rows remain stable, runtime report/runtime state/quarantine are not persisted, operation roundtrip all passed across `11` families, render/export parity passed, and full cutover remains blocked by top-level `nle`, runtime-state persistence, and legacy-compatibility gates.
1. The completed internal NLE baseline is a source-app domain/adapter layer only. It does not reopen native migration, Swift rewrite, QML migration, or a visible Premiere-style UI clone.
2. Runtime NLE adoption evidence covers `gap_delete`, `gap_generate`, `caption_move`, `caption_resize`, `caption_text_edit`, `caption_split`, `caption_range_replace`, `caption_merge`, `caption_delete`, `candidate_confirm`, `marker_edit` for provisional cut-boundary create/delete, output-domain `roughcut_range_edit` for roughcut candidate order/range edits, live editor `diamond` shared-boundary resize, live editor `square_left`/`square_right` boundary-handle resize, live editor segment delete-to-gap, live editor gap-generate, live editor diamond merge, live editor text/smart caption split, live editor STT1/STT2 candidate-confirm routes, timeline inline text commits, shortcut start/end-to-playhead, shortcut split-at-playhead, popup replace-all text commits, quality-review text commits, and partial range replacement commits through NLE dual-write.
3. NLE projection evidence covers final-overlay projection, global-canvas final-only projection, save/export final-caption projection for externalized final SRT/cache rows, NLE final-surface overlap guard, and roughcut saved-candidate render-plan projection.
4. NLE final-surface overlap guard evidence: `output/manual_verification/latest/nle_final_surface_overlap_guard_20260628/final_surface_overlap_guard_report.md`; one-frame final-surface micro-overlap is repaired to a shared boundary when the caption still keeps at least one frame, unfixable overlay/global-canvas overlap is not drawn as two active final captions, and save/export rejects unfixable final overlap before writing final SRT.
5. NLE persistence cutover audit evidence: `output/manual_verification/latest/nle_persistence_cutover_audit_20260628/nle_persistence_cutover_audit.md`; `prep_ready=true`, `persistence_cutover_ready=false`, runtime `NLEProjectState` hydration is proven through a saved roundtrip fixture, disk storage remains clean of `nle`, `nle_snapshot`, `_nle_project_state`, operation roundtrip passed for the then-current 8 NLE dual-write families, and persisted NLE disk-format cutover remains blocked until owner-approved compatibility gates exist. The first audit flagged legacy ID renumbering on `gap_generate`, `caption_split`, `caption_merge`, and `candidate_confirm`.
6. NLE global-canvas projection evidence: `output/manual_verification/latest/nle_global_canvas_final_projection_20260627/global_canvas_projection_report.md`; timeline canvas keeps live STT/subtitle preview rows, while global canvas can receive NLE `global_rows` filtered to final captions only.
7. NLE save/export projection evidence: `output/manual_verification/latest/nle_save_export_projection_cutover_20260628/save_export_projection_report.md`; externalized final SRT/cache rows now use NLE `save_export` final-caption projection while silence gaps stay in vector-canvas gap metadata and STT1/STT2 reference tracks remain separate.
8. NLE roughcut saved-candidate render-plan evidence: `output/manual_verification/latest/nle_roughcut_state_render_plan_cutover_20260628/roughcut_state_render_plan_report.md`; `roughcut_state` routes saved candidate `outputs.render_plan` construction through the NLE snapshot adapter with legacy render-command parity.
9. NLE live editor caption-merge evidence: `output/manual_verification/latest/nle_live_editor_caption_merge_cutover_20260628/caption_merge_cutover_report.md`; diamond merge attempts runtime NLE `caption_merge` dual-write for stable final captions while preserving Taption/source-app QTextDocument merge fallback for STT/live preview rows, NLE rejection, or unsupported shapes.
10. NLE live editor caption-split evidence: `output/manual_verification/latest/nle_live_editor_caption_split_cutover_20260628/caption_split_cutover_report.md`; text/smart split attempts runtime NLE `caption_split` dual-write for stable final captions while preserving Taption/source-app QTextDocument split fallback for STT/live preview rows, NLE rejection, or unsupported shapes.
11. NLE live editor candidate-confirm evidence: `output/manual_verification/latest/nle_live_editor_candidate_confirm_cutover_20260628/candidate_confirm_cutover_report.md`; STT1/STT2 candidate confirmation attempts runtime NLE `candidate_confirm` dual-write after the existing Taption/source-app placement logic computes final rows, while preserving fallback whenever NLE projection would alter source-app timing or sees unsupported rows.
12. NLE operation identity preservation evidence: `output/manual_verification/latest/nle_persistence_identity_preservation_20260628/nle_persistence_cutover_audit.md`; NLE shadow projection now preserves non-generic operation IDs, `candidate_confirm` maps generic `caption_1`/`caption_2` rows back to existing `subtitle_vector_*` identities, live editor block metadata carries `segment_id`, explicit `save-project` uses flushed current editor rows, and the then-current 8 NLE dual-write operation families reopen with `reopened_identity_preserved=true`. Final source-app quick QA evidence: `output/manual_verification/latest/qa_suite_quick_nle_identity_save_project_fix_20260628`.
13. NLE timeline canvas read/projection evidence: `output/manual_verification/latest/nle_timeline_canvas_projection_cutover_20260628/timeline_canvas_projection_report.md`; `TimelineCanvas.update_segments(...)` now normalizes incoming rows through `nle_timeline_canvas_segments_from_editor_rows(...)`, final captions are projected through the NLE `timeline_canvas` surface, STT1/STT2/live subtitle preview lanes remain on the main timeline canvas, explicit silence gaps stay gap rows, global canvas remains final-only, and source-app quick QA passed at `output/manual_verification/latest/qa_suite_quick_nle_timeline_canvas_projection_20260628`.
14. NLE commit-boundary reorder sync evidence: `output/manual_verification/latest/nle_commit_boundary_reorder_sync_20260628/reorder_sync_report.md`; Taption immediate-neighbor `center_reorder_left` / `center_reorder_right` release commits now route through runtime NLE `caption_move` dual-write with release provenance, final overlap `0`, global max active `1`, and legacy fallback on NLE rejection while preserving no per-pixel NLE writes during drag.
15. NLE center-move commit sync evidence: `output/manual_verification/latest/nle_center_move_commit_sync_20260628/center_move_sync_report.md`; safe pure body `center` move release commits now route through runtime NLE `caption_move` dual-write with `commit_boundary=release`, `commit_source=center`, `taption_reorder=false`, final overlap `0`, global max active `1`, and legacy fallback on NLE rejection.
16. NLE complex center commit sync evidence: `output/manual_verification/latest/nle_complex_center_commit_sync_20260628/complex_center_sync_report.md`; Taption-style body `center` release commits for explicit silence gap absorption and previous/next overwrite trim now route through runtime NLE `caption_move` commit adoption with `commit_mode=center_gap_absorb` or `center_overwrite_trim`, final overlap `0`, global max active `1`, STT/live preview route rejection, and legacy fallback on NLE rejection.
17. NLE inline text commit sync evidence: `output/manual_verification/latest/nle_inline_text_commit_sync_20260628/inline_text_commit_report.md`; timeline inline editor text commits now route stable final-caption text changes through runtime NLE `caption_text_edit` with `commit_boundary=release`, `commit_source=timeline_inline_text`, no timing drift, `\u2028` newline save/reopen roundtrip coverage, STT/live preview route rejection, and legacy fallback on NLE rejection.
18. NLE persistence identity matrix refresh evidence: `output/manual_verification/latest/nle_persistence_identity_preservation_inline_text_20260628/nle_persistence_cutover_audit.md`; at that checkpoint the audit included all 9 then-current NLE dual-write operation families, including `caption_text_edit`, with `reopened_identity_preserved=true`, final overlap `0`, max active `1`, and storage still clean of unapproved NLE fields.
19. NLE speaker split text commit sync evidence: `output/manual_verification/latest/nle_speaker_split_text_commit_sync_20260628/speaker_split_sync_report.md`; timeline/live-editor speaker split release commits now route stable final captions through runtime NLE `caption_text_edit` with `commit_boundary=release`, `commit_source=timeline_speaker_split`, `speaker_list` save/reopen preservation, one final multi-speaker row, STT/live preview route rejection, and QTextDocument fallback on NLE rejection.
20. NLE diamond delete commit sync evidence: `output/manual_verification/latest/nle_diamond_delete_commit_sync_20260628/diamond_delete_sync_report.md`; diamond drag delete release commits now route keep-left/keep-right delete-plus-resize results through runtime NLE `caption_move` commit adoption with `commit_boundary=release`, `commit_source=diamond_delete`, final overlap `0`, line-mismatch source-app mapping preserved, and QTextDocument fallback on NLE rejection.
21. NLE speaker drop commit sync evidence: `output/manual_verification/latest/nle_speaker_drop_commit_sync_20260628/speaker_drop_sync_report.md`; same-caption speaker-circle drag/drop release commits now route through runtime NLE `caption_text_edit` with `commit_boundary=release`, `commit_source=timeline_speaker_drop`, ordered `speaker_list` preservation, distinct-caption NLE suppression, and QTextDocument fallback on NLE rejection.
22. NLE smart split commit sync evidence: `output/manual_verification/latest/nle_smart_split_commit_sync_20260628/smart_split_sync_report.md`; timeline smart split release commits now route stable final captions through runtime NLE `caption_split` with `commit_boundary=release`, `commit_source=timeline_smart_split`, final overlap `0`, global max active `1`, and QTextDocument fallback on NLE rejection.
23. NLE speaker change commit sync evidence: `output/manual_verification/latest/nle_speaker_change_commit_sync_20260628/speaker_change_sync_report.md`; direct speaker menu changes on single-block stable final captions now route through runtime NLE `caption_text_edit` with `commit_boundary=release`, `commit_source=timeline_speaker_change`, unchanged text/timing, guarded QTextBlock shape preservation, and fallback on NLE rejection.
24. NLE provisional cut-boundary marker edit evidence: `output/manual_verification/latest/nle_provisional_cut_boundary_marker_edit_20260628/marker_edit_report.md`; right-click provisional cut-boundary create/delete now records runtime NLE `marker_edit` operations with `commit_boundary=release`, `commit_source=provisional_cut_boundary_create/delete`, and before/after provisional marker counts while preserving the existing scan-boundary UI rows and Taption/source-app fallback behavior.
25. NLE shortcut start/end-to-playhead resize evidence: `output/manual_verification/latest/nle_shortcut_resize_commit_sync_20260628/shortcut_resize_report.md`; `_set_segment_start_to_playhead` / `_set_segment_end_to_playhead` now route safe single-block gap-absorb release commits through runtime NLE `caption_resize` with `commit_source=shortcut_start_to_playhead` or `shortcut_end_to_playhead`, final overlap `0`, global max active `1`, and legacy QTextBlock fallback when a new gap must be created, a gap must be extended, NLE rejects the row, or the row is STT/live-preview/unsupported.
26. NLE popup replace-all text commit evidence: `output/manual_verification/latest/nle_popup_replace_all_commit_sync_20260628/popup_replace_all_report.md`; `_replace_text_in_all_subtitles(...)` now routes safe whole-document final-caption text replacements through sequential runtime NLE `caption_text_edit` operations with `commit_source=popup_replace_all`, final overlap `0`, global max active `1`, and legacy QTextDocument fallback when a visible gap block must keep its text, NLE rejects a row, or the row set contains STT/live-preview/unsupported rows.
27. NLE quality-review text commit evidence: `output/manual_verification/latest/nle_quality_review_text_commit_sync_20260628/quality_text_commit_report.md`; `_replace_segment_text_by_line(...)` now routes quality candidate and one-click text replacement commits on stable final captions through runtime NLE `caption_text_edit` with `commit_source=quality_candidate_text`, final overlap `0`, global max active `1`, quality metadata restored after projection reload, and legacy QTextDocument fallback for STT/live preview, unchanged text, unsupported rows, or NLE rejection.
28. NLE partial range replacement commit sync evidence: `output/manual_verification/latest/nle_partial_range_replace_commit_sync_20260628/range_replace_report.md`; `clear_segments_in_range(...)` / `insert_partial_segments(...)` now route stable final-caption partial replacements through runtime NLE `caption_range_replace` with `commit_boundary=release`, `commit_source=partial_insert_range_replace`, target-range preservation guards, final overlap `0`, global max active `1`, fallback on NLE rejection, and a refreshed persistence audit covering all 10 then-current NLE dual-write operation families.
29. NLE shortcut split-at-playhead commit sync evidence: `output/manual_verification/latest/nle_shortcut_split_commit_sync_20260628/shortcut_split_report.md`; `_split_at_playhead_or_cut(...)` now routes stable final-caption playhead insert/split commits through runtime NLE `caption_split` with `commit_boundary=release`, `commit_source=shortcut_split_at_playhead`, final overlap `0`, global max active `1`, focus handoff to the inserted row, and legacy QTextDocument fallback for STT/live preview, gap rows, unsupported rows, invalid split positions, or NLE rejection.
30. NLE commit-boundary mutable sync closeout evidence: `output/manual_verification/latest/nle_shortcut_split_commit_sync_20260628/shortcut_split_report.md` plus `.agents/sentinel/handoffs/20260628-075500-nle-remaining-release-source-audit.md`; after the shortcut split slice and fresh support audit, no further known safe existing-family release/commit subtitle mutation source remains promoted in `ACTION_ITEMS.md`.
31. Persisted NLE project fields remain gated, and broader persistence/save/render/export ownership cleanup remains gated.
32. NLE persistence cutover audit now includes a save/render/export parity gate. `tools/audit_nle_persistence_cutover.py` builds a roughcut/export fixture, reopens it through legacy project I/O, verifies disk storage stays clean of `nle`, `nle_snapshot`, and `_nle_project_state`, and requires stable NLE projection across `source_subtitles`, `final_overlay`, `global_canvas`, `roughcut_sidecar`, and `exported_assets` before reporting `prep_ready=true`. Evidence: `output/manual_verification/latest/nle_persistence_render_export_gate_20260628/nle_persistence_cutover_audit.md`; persisted NLE disk-format cutover remains blocked by owner approval and compatibility gates.
33. NLE persistence cutover audit now includes provisional cut-boundary `marker_edit` in the save/reopen operation matrix. The audit verifies all 11 current NLE dual-write operation families, including marker preservation after legacy project roundtrip, while keeping persisted NLE disk-format cutover blocked. Evidence: `output/manual_verification/latest/nle_marker_edit_persistence_gate_20260628/nle_persistence_cutover_audit.md`.
34. NLE runtime owner-map audit tooling was added. `tools/audit_nle_runtime_owner_map.py` verifies 23 current release/commit mutation owners across 11 NLE operation families, confirms runtime change applied `false`, and keeps blocked candidates explicit for persisted NLE disk fields, per-pixel NLE writes, and QML/GPU timeline default surfaces. Evidence: `output/manual_verification/latest/nle_runtime_owner_map_audit_20260628/nle_runtime_owner_map_audit.md`; next adoption of any new mutation source still requires fresh owner-map plus Taption release-commit/no-per-pixel-write/final-overlap/global-canvas/save-reopen proof.
35. NLE adapter/cache consistency audit tooling was added. `tools/audit_nle_adapter_consistency.py` proves the current source-app adapter keeps `_nle_project_state` runtime-only across repeated save/reopen cycles, verifies same-cache state identity, cache-clear rehydration, runtime marker non-persistence, clean storage payloads, final invalid/non-monotonic/overlap `0/0/0`, global max-active `1`, and `_PROJECT_FILE_CACHE` LRU limit `4/4`. Evidence: `output/manual_verification/latest/nle_adapter_consistency_audit_20260628/nle_adapter_consistency_audit.md`; runtime behavior changed `false`, and persisted NLE disk fields/per-pixel writes/QML defaults remain blocked.
36. NLE operation journal/undo contract audit tooling was added. `tools/audit_nle_operation_journal.py` covers all 11 current dual-write operation families (`gap_delete`, `gap_generate`, `caption_move`, `caption_resize`, `caption_text_edit`, `caption_split`, `caption_range_replace`, `caption_merge`, `caption_delete`, `candidate_confirm`, `marker_edit`), verifies `commit_boundary=release` plus commit-source metadata on both the operation and undo snapshot, proves final invalid/non-monotonic/overlap `0/0/0`, global max-active `1`, and confirms operation/undo/runtime NLE schemas are not persisted into legacy project storage. Evidence: `output/manual_verification/latest/nle_operation_journal_audit_20260628/nle_operation_journal_audit.md`; runtime behavior changed `false`, and persisted operation journals, runtime undo/redo UI changes, per-pixel NLE writes, and QML/GPU timeline defaults remain blocked. NAS HeyDealer first-180s regression also passed after the slice at `output/manual_verification/latest/nle_operation_journal_nas_heydealer_20260628/acceptance/reference_benchmark_acceptance.md`.
37. Runtime in-memory NLE operation journal recording was added to the dual-write commit boundary. `NLEProjectState` now keeps a bounded runtime-only journal tail for the current session, and every current dual-write operation family appends release metadata, commit source, undo snapshot id, target ids, projected count, and final projection stability counts after successful projection sync. Evidence: `output/manual_verification/latest/nle_runtime_operation_journal_20260628/nle_operation_journal_audit.md`; all 11 families recorded runtime journal entries, storage stayed clean of operation/undo/journal/runtime NLE schemas, final invalid/non-monotonic/overlap stayed `0/0/0`, global max-active stayed `1`, and persisted operation journals/runtime undo-redo UI/per-pixel NLE writes/QML defaults remain blocked. NAS HeyDealer first-180s regression also passed after the slice at `output/manual_verification/latest/nle_runtime_operation_journal_nas_heydealer_20260628/acceptance/reference_benchmark_acceptance.md`.
38. NLE preview/skimming frame-cache provenance was added. `core/runtime/preview_frame_cache.py` now writes a sidecar manifest for preview frame thumbnails with `purpose=editor_preview_skimming`, `evidence_role=user_preview_only`, `cut_boundary_evidence=false`, `ui_thread_decode_allowed=false`, source-fps frame-grid metadata, and a bounded temp `Preview/FrameThumbnails` location. Evidence: `output/manual_verification/latest/nle_preview_skimming_cache_audit_20260628/nle_preview_skimming_cache_audit.md`; nearest-frame cache lookup, async worker scheduling, and video player preview cache tests passed, while UI layout/labels/menus/popups, cut-boundary evidence ownership, QML/GPU defaults, and sync UI-thread miss decode remain blocked.
39. NLE preview/skimming trace events were added. `VideoPlayerSurfaceMixin` now emits best-effort async `TraceLogger` events for preview cache hit, miss, schedule, and ready states with `source=editor_preview_skimming`, `evidence_role=user_preview_only`, `cut_boundary_evidence=false`, `ui_thread_decode_allowed=false`, cache status, frame, and exact fps numerator/denominator fields. Evidence: `output/manual_verification/latest/nle_preview_skimming_trace_audit_20260628/nle_preview_skimming_cache_audit.md`; the existing preview seek throttle stays in place, UI/layout behavior does not change, and preview thumbnails remain prohibited as cut-boundary evidence.
40. NLE confirmed cut-boundary decision trace events were added. `core/cut_boundary.py` now emits best-effort async `confirmed_cut_split_snap` events for confirmed visual-cut split/snap/drop decisions with `event_type=cut_boundary_decision`, `decision`, `provisional_frame`, `drop_reason`, source segment identity, start/end frame fields, and exact fps numerator/denominator fields. Evidence: `output/manual_verification/latest/nle_confirmed_cut_trace_audit_20260628/trace_log_bundle_audit.md`; focused cut-boundary/trace tests passed, and NAS HeyDealer first-180s regression accepted at `output/manual_verification/latest/nle_confirmed_cut_trace_nas_heydealer_20260628/acceptance/reference_benchmark_acceptance.md` with raw/final/reference `58/56/89`, quality/text/timing `93.766/94.267/0.5808s`, final invalid/non-monotonic/overlap `0/0/0`, global max active `1`, and no FFmpeg threshold, UI/layout, subtitle quality policy, or persisted-NLE behavior change.
41. Fixed cut-boundary frame `2766`/`2677` fixture gates were added. `tools/verify_cut_boundary_source_fps_scout.py` now supports probe/frame-extract timeouts, `--fps-override`, `--pipe-max-fps`, markdown evidence, and metadata-only frame-grid fallback for the local iCloud fixture when decoder access stalls; `tests/test_cut_boundary_fixture_2766_2677.py` guards exact 60000/1001fps frame-grid preservation plus split/snap rows that do not cross the target frames. Evidence: `output/manual_verification/latest/nle_fixed_cut_boundary_fixture_gate_20260628/source_fps_scout.md`; target frames were preserved with `candidate_detected=false`, so this is metadata/frame-grid proof rather than visual detection proof. NAS HeyDealer first-180s regression after the slice accepted at `output/manual_verification/latest/nle_fixed_cut_boundary_fixture_gate_nas_heydealer_20260628/acceptance/reference_benchmark_acceptance.md` with raw/final/reference `55/57/89`, quality/text/timing `93.955/94.867/0.5536s`, final invalid/non-monotonic/overlap `0/0/0`, global max active `1`, and a separate latency caveat: WhisperKit worker timeout/fallback dominated STT1/STT2/word precision.
42. Fixed cut-boundary visual evidence classification was added. `tools/verify_cut_boundary_source_fps_scout.py` now reports `visual_candidate_status`, `visual_evidence_available`, `visual_detection_summary`, and an optional `--require-visual-detection` strict gate so frame-grid preservation cannot be mistaken for visual detector proof. Evidence: `output/manual_verification/latest/nle_fixed_cut_boundary_visual_evidence_gate_20260628/source_fps_scout.md`; decoder extraction succeeded, frames `2766` and `2677` were both `preserved_only`, `visual_evidence_available=true`, `strict_visual_detection_passed=false`, `visual_candidate_missing_count=2`, and `acceptance_claim=frame_grid_preservation_with_visual_candidate_gap`. The strict artifact `output/manual_verification/latest/nle_fixed_cut_boundary_visual_evidence_gate_20260628_strict/source_fps_scout.md` fails as expected with `--require-visual-detection`, blocking visual-detection claims until a separate detector-tuning slice is proven. `tests/test_pipeline_cut_boundary_cache.py` now reads saved project payloads through `read_project_storage_payload(...)`, matching the current binary/json project I/O instead of assuming UTF-8 JSON.
43. Fixed cut-boundary visual transition window audit tooling was added. `tools/audit_cut_boundary_visual_window.py` ranks visual transition scores around target frames without changing runtime detector thresholds or subtitle policy; `tests/test_cut_boundary_visual_window_audit.py` covers detected-best, preserved-only blocked, and markdown guardrail cases. Evidence: `output/manual_verification/latest/nle_cut_boundary_visual_window_audit_20260628/cut_boundary_visual_window_audit.md`; strict targets detected `false`, target best count `0/2`, frame `2766` ranked `4` with score `2.059` while best nearby frame `2769` scored `2.715`, and frame `2677` ranked `2` with score `1.997` while frame `2676` scored `71.932` and was detected. This is read-only frame-semantics/detector-tuning evidence only; threshold relaxation, subtitle quality/STT policy changes, UI/QML, persisted NLE fields, and App Store work remain blocked.
44. Fixed cut-boundary frame-semantics audit tooling was added. `tools/audit_cut_boundary_frame_semantics.py` reads the existing visual-window JSON and classifies target-frame detection gaps versus detected-neighbor semantic conflicts without decoding media or changing runtime behavior; `tests/test_cut_boundary_frame_semantics_audit.py` covers detected target transitions, detected-neighbor-before-target conflicts, and markdown guardrails. Evidence: `output/manual_verification/latest/nle_cut_boundary_frame_semantics_audit_20260628/cut_boundary_frame_semantics_audit.md`; `frame_semantics_review_required=true`, semantic mismatch count `1`, target detection gaps `2`, detected-neighbor conflict count `1`, detector-tuning candidate count `1`, and `runtime_change_allowed=false`. Frame `2766` remains `target_detection_gap`; frame `2677` is `detected_neighbor_before_target` because the strongest detected transition is `2675 -> 2676`. NAS HeyDealer first-180s regression after the slice accepted at `output/manual_verification/latest/nle_frame_semantics_nas_heydealer_20260628/acceptance/reference_benchmark_acceptance.md`; elapsed was `179.579s`, final invalid/non-monotonic/overlap stayed `0/0/0`, global max-active stayed `1`, and `output/manual_verification/latest/stt_worker_timeout_compare_frame_semantics_nas_20260628/stt_worker_timeout_audit.md` reports `timeout_detected=false`.
45. Fixed cut-boundary fixture convention/contact-sheet audit tooling was added. `tools/audit_cut_boundary_fixture_convention.py` reads the frame-semantics JSON, extracts the actual fixed-fixture frames, writes PNG contact sheets, and compares expected target-pair versus strongest-neighbor-pair visual deltas without changing runtime detector thresholds or subtitle policy; `tests/test_cut_boundary_fixture_convention_audit.py` covers neighbor-before-target convention review, plain detector-evidence gaps, and markdown/contact-sheet guardrails. Evidence: `output/manual_verification/latest/nle_cut_boundary_fixture_convention_audit_20260628/cut_boundary_fixture_convention_audit.md`; contact sheets `target_2677_frame_contact_sheet.png` and `target_2766_frame_contact_sheet.png` were created, `fixture_label_or_boundary_convention_review_required=true`, label/boundary review count `1`, detector evidence required count `1`, and runtime change allowed `false`. Frame `2677` expected pair `2676 -> 2677` has mean delta `2.381499`, while strongest pair `2675 -> 2676` has mean delta `72.849699` and ratio `30.589851`, so the target convention remains blocked before detector threshold tuning. Current NAS preflight after the slice passed at `output/manual_verification/latest/nle_fixture_convention_nas_preflight_20260628/reference_fixture_availability.md`.
46. Fixed cut-boundary fixture target correction tooling was added. `tools/audit_cut_boundary_fixture_target_correction.py` reads the prior convention audit and emits the corrected QA target map without changing runtime behavior; `tests/test_cut_boundary_fixture_target_correction.py` covers the `2677 -> 2676` correction and markdown guardrails. The source-fps scout, visual-window audit, and fixed-fixture pytest defaults now use current target frames `2766,2676` and pairs `2765:2766,2675:2676`; the historical `2677` target is superseded as one frame late for this fixture. Evidence: `output/manual_verification/latest/nle_cut_boundary_fixture_target_correction_20260628/cut_boundary_fixture_target_correction.md`, `output/manual_verification/latest/nle_corrected_target_source_fps_scout_20260628/source_fps_scout.md`, `output/manual_verification/latest/nle_corrected_target_visual_window_audit_20260628/cut_boundary_visual_window_audit.md`, `output/manual_verification/latest/nle_corrected_target_frame_semantics_audit_20260628/cut_boundary_frame_semantics_audit.md`, and `output/manual_verification/latest/nle_corrected_target_fixture_convention_audit_20260628/cut_boundary_fixture_convention_audit.md`; frame `2676` is detected/target-best/convention-clean, semantic mismatch count is `0`, and only frame `2766` remains detector-evidence work. NAS preflight after the slice passed at `output/manual_verification/latest/nle_target_correction_nas_preflight_20260628/reference_fixture_availability.md`. Runtime detector thresholds, subtitle quality/STT policy, UI/QML, persisted NLE fields, App Store work, packaging, signing, upload, and DMG behavior were not changed.
47. Fixed cut-boundary frame `2766` detector-evidence robustness audit tooling was added. `tools/audit_cut_boundary_detector_evidence_robustness.py` evaluates fixed target pairs across existing visual scorer modes and widths without changing runtime behavior; `tests/test_cut_boundary_detector_evidence_robustness.py` covers weak visual changes, strong detected cuts, markdown output, and guardrails. Evidence: `output/manual_verification/latest/nle_cut_boundary_2766_detector_robustness_20260628/cut_boundary_detector_evidence_robustness.md`; frame `2766` is classified as `weak_visual_change_not_threshold_candidate` even across `fast4,cross5,full9` and widths `320,480,960,1920`, with best score `3.812`, best hits `0`, best pixel `0.034849`, and best motion `1.315`, while frame `2676` remains `visual_detection_available`. Detector tuning candidate count is `0`, threshold relaxation allowed `false`, and runtime change allowed `false`; preserve `2766` as frame-grid/marker evidence or revisit fixture truth rather than lowering detector thresholds from this fixture alone.
48. Fixed cut-boundary preserved-marker policy audit tooling was added. `tools/audit_cut_boundary_preserved_marker_policy.py` merges the corrected source-fps scout and detector-robustness artifacts into an explicit NLE marker policy contract without changing runtime behavior; `tests/test_cut_boundary_preserved_marker_policy.py` covers preserved-marker classification, detector-tuning review blocking, markdown guardrails, and split/snap guard references. Evidence: `output/manual_verification/latest/nle_preserved_marker_policy_20260628/cut_boundary_preserved_marker_policy.md`; frame `2676` is `visual_marker_confirmed`, frame `2766` is `preserved_marker_required`, review required frames are empty, confirmed cuts remain point evidence rather than clip spans, and final subtitle guards require invalid/non-monotonic/overlap `0/0/0`, global max active `1`, and no rows crossing confirmed markers through `tests/test_cut_boundary_fixture_2766_2677.py::test_confirmed_fixture_cut_frames_split_snap_without_crossing_rows`. Runtime detector thresholds, subtitle quality/STT policy, UI/QML, persisted NLE fields, App Store work, clip-span mapping for point markers, per-pixel NLE writes, and visual-threshold lowering from preserved markers remain blocked. NAS preflight after the slice passed at `output/manual_verification/latest/nle_preserved_marker_policy_nas_preflight_20260628/reference_fixture_availability.md`.
49. Roughcut output-domain range edit NLE operation support was added. `core/project/nle_dual_write.py` now records roughcut candidate order/range edits as `roughcut_range_edit` operations with `time_domain=output`, release commit metadata, undo snapshot metadata, runtime-only `NLEProjectState` journal entries, unchanged final caption rows, and clean legacy storage. Evidence: `output/manual_verification/latest/nle_roughcut_range_edit_operation_journal_20260628/nle_operation_journal_audit.md` reports all `12` current operation families ready, runtime journals `12`, storage clean `12`, final invalid/non-monotonic/overlap `0/0/0`, and global max active `1`; `output/manual_verification/latest/nle_roughcut_range_edit_owner_map_20260628/nle_runtime_owner_map_audit.md` reports owners `24/24` covered. NAS HeyDealer first-180s regression after the slice accepted at `output/manual_verification/latest/nle_roughcut_range_edit_nas_heydealer_20260628/acceptance/reference_benchmark_acceptance.md` with elapsed `51.429s`, raw/final/reference `58/56/89`, quality/text/timing `93.766/94.267/0.5808s`, final invalid/non-monotonic/overlap `0/0/0`, final last end/duration bound `180.0/180.0`, short/long `0/0`, and global max active `1`. Runtime detector thresholds, subtitle quality/STT policy, UI/QML, persisted NLE fields, App Store work, per-pixel NLE writes, and roughcut UI schema changes remain blocked.
50. Project IO save/load trace events were added for the NLE save/load diagnostic lane. `core/project/project_io.py` now emits best-effort async `project_file_save` and `project_file_open` trace events after successful save, disk read, and cache-hit read, using basename plus path hash instead of raw project paths. The events expose `event_type`, cache-hit source, elapsed time, NLE runtime-state attachment, storage-clean flags, payload codec/compression, and stripped runtime-key count while keeping `.aissproj` storage clean of runtime NLE state. Evidence: `output/manual_verification/latest/project_io_trace_contract_20260628/project_io_trace_contract.md` reports `passed=true`, event count `3`, save/disk-open/cache-hit counts `1/1/1`, raw path leak `false`, storage clean `true`, and disk/cache NLE state attached `true/true`. UI/layout behavior, subtitle quality policy, persisted NLE disk-format, App Store packaging/signing/upload, DMG behavior, and per-pixel NLE writes remain unchanged.
51. NLE operation journal append trace events were added for the runtime-only operation journal lane. `record_nle_operation_journal_entry(...)` now emits best-effort async `nle_operation_journal_append` events after successful journal append, with operation id, kind, family, time domain, commit boundary/source, undo snapshot id, projected count, final stability counts, and runtime journal counts while omitting caption text, raw project paths, and raw `target_ids`. Evidence: `output/manual_verification/latest/nle_operation_journal_trace_audit_20260628/nle_operation_journal_audit.md` reports all `12` current operation families traced, `operation_trace_event_count=12`, trace contract ok `true`, storage clean `12`, final invalid/non-monotonic/overlap `0/0/0`, and global max-active `1`. NAS HeyDealer first-180s regression after the slice accepted at `output/manual_verification/latest/nle_operation_journal_trace_nas_heydealer_20260628/acceptance/reference_benchmark_acceptance.md` with elapsed `52.699s`, raw/final/reference `58/56/89`, quality/text/timing `93.766/94.267/0.5808s`, final `0/0/0`, global max-active `1`, and timeout detected `false`. UI/layout behavior, subtitle quality policy, persisted NLE disk-format, App Store packaging/signing/upload, DMG behavior, runtime undo/redo UI, and per-pixel NLE writes remain unchanged.
52. NLE drag commit-boundary guard evidence was added for Taption-style preview-only timeline body drag. `tests/test_editor_timeline_drag_release.py` now proves a center body drag updates the canvas preview during mouse move while editor rows remain unchanged and NLE `caption_move` dual-write call count stays `0`; release then commits the same final rows through NLE exactly once. Broader validation also restored direction-aware diamond shared-boundary release ordering in `ui/editor/ux/timeline_input.py`, so left/right diamond drags remain gap-free. `tools/audit_nle_runtime_owner_map.py` now reports this as commit-boundary guard `timeline_center_drag_preview_only_until_release` with contract `taption_preview_only_until_release_commit`. Evidence: `output/manual_verification/latest/nle_drag_commit_boundary_guard_20260628/nle_runtime_owner_map_audit.md` reports owner map ready `true`, owners `24/24`, commit-boundary guards `1/1`, and missing guards `0`. NAS HeyDealer first-180s regression after the slice accepted at `output/manual_verification/latest/nle_drag_commit_boundary_guard_nas_heydealer_20260628/acceptance/reference_benchmark_acceptance.md` with elapsed `53.919s`, raw/final/reference `58/56/89`, quality/text/timing `93.766/94.267/0.5808s`, final `0/0/0`, global max-active `1`, and timeout detected `false`. Jammini scout `.agents/sentinel/handoffs/20260628-161800-next-nle-taption-ux-scout.md` recommended preview/skimming cache-miss UI-thread block-prevention tooling as the next safe candidate; Dex deferred that until after this commit-boundary gate.
53. NLE preview/skimming cache-miss block-free evidence was added. `tests/test_video_player_widget.py` now proves a slow cache-miss frame preparation worker starts while `VideoPlayerWidget.preview_seek()` returns in under `50ms`, without falling back to synchronous thumbnail paint on the UI thread. `tools/audit_nle_preview_skimming_cache.py` now requires a `cache_miss_thread_contract` covering worker-thread scheduling, in-worker decode, the named `video-preview-frame-cache` worker, worker-active reentry guard, and signal-based ready paint. Evidence: `output/manual_verification/latest/nle_preview_skimming_cache_miss_block_free_20260628/nle_preview_skimming_cache_audit.md`; contract ready `true`, preview provenance remains `editor_preview_skimming` / `user_preview_only`, `cut_boundary_evidence=false`, and `ui_thread_decode_allowed=false`. Jammini prep `.agents/sentinel/handoffs/20260628-163300-nle-preview-skimming-cache-miss-prep.md` was accepted as the bounded support input. NAS HeyDealer first-180s regression after the guard accepted at `output/manual_verification/latest/nle_preview_skimming_cache_miss_nas_heydealer_20260628/acceptance/reference_benchmark_acceptance.md` with elapsed `45.744s`, raw/final/reference `58/56/89`, quality/text/timing `93.766/94.267/0.5808s`, final `0/0/0`, global max-active `1`, and timeout detected `false`.
54. NLE viewport zoom decoupling evidence was added. `tests/test_timeline_wheel_zoom_decoupling.py` now proves timeline Ctrl-wheel zoom and global-canvas wheel scroll update only viewport scale/scroll state while preserving canvas/global subtitle rows and avoiding runtime NLE operation journal appends. `tools/audit_nle_viewport_zoom_decoupling.py` statically checks `TimelineWidget.wheelEvent`, `TimelineWidget._apply_zoom`, `GlobalCanvas.wheelEvent`, and `TimelineCanvas.set_zoom` for forbidden primary-model/NLE write calls or assignments. Evidence: `output/manual_verification/latest/nle_viewport_zoom_decoupling_20260628/nle_viewport_zoom_decoupling.md`; audit `ready=true`, viewport-only contract `true`, model/NLE writes allowed `false/false`, forbidden calls/assignments `0`, and runtime change applied `false`. Jammini scout `.agents/sentinel/handoffs/20260628-164700-next-nle-taption-runtime-contract-scout.md` was accepted with scope narrowed to test/audit hardening. NAS HeyDealer generation validation was not run because this view-only slice does not touch STT/VAD/subtitle generation/final rows.
55. NLE playhead-jump isolation evidence was added. `tests/test_timeline_playhead_jump_isolation.py` now proves global minimap click and timeline global seek emit scrub/playhead updates while preserving canvas/global subtitle rows and avoiding runtime NLE operation journal appends or project saves; the editor scrub path updates the timeline playhead plus lightweight preview seek without immediate subtitle validation/rescan, dirty marking, segment timing mutation, or dual-write calls. `tools/audit_nle_playhead_jump_isolation.py` statically checks `GlobalCanvas.mousePressEvent`, `TimelineWidget._on_global_seek`, and `EditorTimelineVideoMixin._on_scrub` for forbidden validation/save/NLE write calls or subtitle-row assignments. Evidence: `output/manual_verification/latest/nle_playhead_jump_isolation_20260628/nle_playhead_jump_isolation.md`; audit `ready=true`, playhead-jump view-only contract `true`, model validation/project save/NLE writes allowed `false/false/false`, forbidden calls/assignments `0`, and runtime change applied `false`. Jammini scout `.agents/sentinel/handoffs/20260628-165700-next-nle-taption-runtime-contract-scout.md` was accepted with scope narrowed to test/audit hardening. NAS HeyDealer generation validation was not run because this view-only slice does not touch STT/VAD/subtitle generation/final rows.
56. NLE time-window/fit-to-view decoupling evidence was added. `tests/test_timeline_time_window_decoupling.py` now proves `fit_to_view`, explicit time-window display, and the saved-preference 10-second edit-window path update viewport scale/scroll/global viewport state while preserving canvas/global subtitle rows and avoiding runtime NLE operation journal appends or project saves. `tools/audit_nle_time_window_view_decoupling.py` statically checks `TimelineWidget.fit_to_view`, `TimelineWidget.schedule_fit_to_view`, `TimelineTimeWindowMixin.show_time_window_seconds`, `_apply_edit_window_seconds`, and `show_ten_second_edit_window` for forbidden validation/save/NLE write calls or subtitle-row assignments. Evidence: `output/manual_verification/latest/nle_time_window_view_decoupling_20260628/nle_time_window_view_decoupling.md`; audit `ready=true`, view-window-only contract `true`, model validation/project save/NLE writes allowed `false/false/false`, forbidden calls/assignments `0`, and runtime change applied `false`. Jammini scout `.agents/sentinel/handoffs/20260628-070859-next-nle-taption-runtime-contract-scout.md` recommended double-click repaint suppression; Dex deferred that repaint-optimization scope due prior dirty-rect/repaint lessons and landed the narrower time-window decoupling guard instead. NAS HeyDealer generation validation was not run because this view-only slice does not touch STT/VAD/subtitle generation/final rows.
57. NLE inline edit entry contract evidence was added. `TimelineInlineEditMixin.start_inline_edit` now emits a best-effort `timeline_inline_edit_entry` trace event for Taption-style double-click/inline-edit entry with no caption text payload, no raw target IDs, no NLE operation journal append, no project save, and no primary subtitle validation/rescan until the actual text commit. The existing text commit route remains the `caption_text_edit` release commit. `tests/test_timeline_hit_targets.py` proves normal inline-edit entry preserves canvas rows, emits no text-change signal, avoids NLE dual-write/journal calls, and writes only the trace event; `tools/audit_nle_inline_edit_entry_contract.py` statically guards the entry contract. Evidence: `output/manual_verification/latest/nle_inline_edit_entry_contract_20260628/nle_inline_edit_entry_contract.md`. NAS HeyDealer first-180s regression after the slice accepted at `output/manual_verification/latest/nle_inline_edit_entry_nas_heydealer_20260628/acceptance/reference_benchmark_acceptance.md` with elapsed `46.481s`, raw/final/reference `58/56/89`, quality/text/timing `93.766/94.267/0.5808s`, final `0/0/0`, final last end/duration bound `180.0/180.0`, global max-active `1`, and timeout detected `false`. Jammini scout `.agents/sentinel/handoffs/20260628-071656-next-concrete-nle-taption-slice.md` recommended duration-bound trim enforcement for a future mutable-edit slice; Dex deferred that broader behavior change for a separate owner-map/test pass. UI/layout behavior, subtitle quality policy, STT/default-cache policy, persisted NLE disk-format, App Store packaging/signing/upload, DMG behavior, runtime undo/redo UI, and per-pixel NLE writes remain unchanged.
58. NLE dual-write duration-bound trim enforcement was added. `core/project/project_context.py` now exposes `trim_editor_rows_to_project_duration(...)`, and `core/project/nle_dual_write.py` applies it before runtime NLE state sync and raw `editor_state` rebuild for `11/11` row-producing release-commit dual-write families. `tests/test_project_nle_dual_write.py` proves a caption moved past the project tail clamps to the duration bound in legacy rows, runtime NLE rows, and raw vector storage, and a beyond-duration candidate confirm row is dropped before storage. `tools/audit_nle_dual_write_duration_bound.py` statically/dynamically guards the contract; evidence: `output/manual_verification/latest/nle_dual_write_duration_bound_20260628/nle_dual_write_duration_bound.md` with owner coverage `11/11`, dynamic checks `caption_move_tail_clamp` and `candidate_confirm_late_drop` passing, final invalid/non-monotonic/overlap `0/0/0`, and global max-active `1`. NAS HeyDealer first-180s regression after the slice accepted at `output/manual_verification/latest/nle_dual_write_duration_bound_nas_heydealer_20260628/acceptance/reference_benchmark_acceptance.md` with elapsed `45.946s`, raw/final/reference `58/56/89`, quality/text/timing `93.766/94.267/0.5808s`, final last end/duration bound `180.0/180.0`, short/long `0/0`, global max-active `1`, and timeout detected `false` at `output/manual_verification/latest/stt_worker_timeout_compare_nle_duration_bound_nas_20260628/stt_worker_timeout_audit.md`. UI/layout behavior, subtitle quality policy, STT/default-cache policy, persisted NLE disk-format, App Store packaging/signing/upload, DMG behavior, runtime undo/redo UI, and per-pixel NLE writes remain blocked/unchanged.
59. NLE gap-delete sequence policy was made explicit as `remove_gap_row_no_ripple`. `core/project/nle_dual_write.py` now records this policy on the gap-delete operation metadata, undo snapshot metadata/UI ref, and runtime `NLEProjectState` metadata while preserving the existing behavior that removes only the explicit gap row and does not silently ripple adjacent captions. `tests/test_project_nle_dual_write.py` and `tests/test_nle_gap_delete_sequence_policy_audit.py` prove legacy rows, runtime NLE rows, and raw vector storage keep adjacent caption timing unchanged, storage stays clean of runtime NLE fields, and final invalid/non-monotonic/overlap plus global max-active remain `0/0/0` and `1`. Evidence: `output/manual_verification/latest/nle_gap_delete_sequence_policy_20260628/nle_gap_delete_sequence_policy.md`. NAS HeyDealer first-180s regression after the slice accepted at `output/manual_verification/latest/nle_gap_delete_sequence_policy_nas_heydealer_20260628/acceptance/reference_benchmark_acceptance.md` with elapsed `47.979s`, raw/final/reference `58/56/89`, quality/text/timing `93.766/94.267/0.5808s`, final last end/duration bound `180.0/180.0`, short/long `0/0`, global max-active `1`, and timeout detected `false` at `output/manual_verification/latest/stt_worker_timeout_compare_nle_gap_delete_policy_nas_20260628/stt_worker_timeout_audit.md`. Jammini scout `.agents/sentinel/handoffs/20260628-074703-next-nle-taption-runtime-contract-scout.md` was accepted only for the owner path focus; its ripple premise was rejected and annotated in the handoff. UI/layout behavior, subtitle quality policy, STT/default-cache policy, persisted NLE disk-format, App Store packaging/signing/upload, DMG behavior, runtime undo/redo UI, and per-pixel NLE writes remain blocked/unchanged.
60. NLE projection metadata preservation was added for dual-write operations. `core/project/nle_dual_write.py` and `core/project/nle_operations.py` now deep-copy retimed/manual/sorted/shadow/serialized projection rows so nested product metadata cannot be shared or lost during runtime NLE-to-legacy projection. `tests/test_project_nle_dual_write.py` and `tests/test_nle_projection_metadata_preservation_audit.py` prove caption move preserves quality/STT candidate metadata, caption merge preserves kept-row metadata, and caption split preserves child speaker/words metadata while keeping the existing manual-quality removal policy for newly edited split text. Evidence: `output/manual_verification/latest/nle_projection_metadata_preservation_20260628/nle_projection_metadata_preservation.md`. NAS HeyDealer first-180s regression after the slice accepted at `output/manual_verification/latest/nle_projection_metadata_preservation_nas_heydealer_20260628/acceptance/reference_benchmark_acceptance.md` with elapsed `45.188s`, raw/final/reference `58/56/89`, quality/text/timing `93.766/94.267/0.5808s`, final invalid/non-monotonic/overlap `0/0/0`, final last end/duration bound `180.0/180.0`, short/long `0/0`, global max-active `1`, and timeout detected `false` at `output/manual_verification/latest/stt_worker_timeout_compare_nle_projection_metadata_nas_20260628/stt_worker_timeout_audit.md`. Jammini scout `.agents/sentinel/handoffs/20260628-080426-next-nle-taption-runtime-contract-scout.md` was accepted with scope narrowed away from arbitrary legacy custom schema expansion. UI/layout behavior, subtitle quality policy, STT/default-cache policy, arbitrary legacy custom schema, persisted NLE disk-format, App Store packaging/signing/upload, DMG behavior, runtime undo/redo UI, and per-pixel NLE writes remain blocked/unchanged.
61. NLE cut marker point-evidence projection was added for marker edit dual-write. `core/project/nle_dual_write.py` now normalizes confirmed/provisional cut markers as point evidence before rebuilding legacy editor context, strips clip-span mapping keys, records `point_evidence_no_clip_span` on operation/undo/runtime metadata, and keeps clip boundaries unchanged. `tests/test_project_nle_dual_write.py` and `tests/test_nle_cut_marker_point_projection_audit.py` prove frames `2766,2676` remain marker points with no `start/end/clip_span` leakage, final invalid/non-monotonic/overlap `0/0/0`, and global max-active `1`. Evidence: `output/manual_verification/latest/nle_cut_marker_point_projection_20260628/nle_cut_marker_point_projection.md`. NAS HeyDealer first-180s regression after the slice accepted at `output/manual_verification/latest/nle_cut_marker_point_projection_nas_heydealer_20260628/acceptance/reference_benchmark_acceptance.md` with elapsed `45.036s`, raw/final/reference `58/56/89`, quality/text/timing `93.766/94.267/0.5808s`, final invalid/non-monotonic/overlap `0/0/0`, final last end/duration bound `180.0/180.0`, short/long `0/0`, global max-active `1`, and timeout detected `false` at `output/manual_verification/latest/stt_worker_timeout_compare_nle_cut_marker_point_projection_nas_20260628/stt_worker_timeout_audit.md`. Jammini scout `.agents/sentinel/handoffs/20260628-082222-next-nle-taption-runtime-contract-scout.md` was accepted with target-frame correction from stale `2677` to `2676`. UI/layout behavior, subtitle quality policy, STT/default-cache policy, detector thresholds, persisted NLE disk-format, App Store packaging/signing/upload, DMG behavior, runtime undo/redo UI, and per-pixel NLE writes remain blocked/unchanged.
62. NLE relink/proxy preview-cache non-destructive contract was added. `core/runtime/preview_frame_cache.py` now writes path-independent media identity fields into preview thumbnail manifests and, after a direct path cache miss, can perform a bounded manifest scan that reuses an existing thumbnail only when media identity, fps, frame, width, preview-only provenance, and cut-boundary exclusion all match. Proxy/transcoded files with different source identity are blocked from reuse, and the cache file is not moved or deleted. Evidence: `output/manual_verification/latest/nle_relink_preview_cache_contract_20260628/nle_relink_preview_cache_contract.md`; relink identity matches `true`, relink hit reuses original cache `true`, proxy hit blocked `true`, cached still exists `true`, and no UI layout or persisted project schema change was applied. NAS HeyDealer first-180s regression after the slice accepted at `output/manual_verification/latest/nle_relink_preview_cache_nas_heydealer_20260628/acceptance/reference_benchmark_acceptance.md` with elapsed `45.515s`, raw/final/reference `58/56/89`, quality/text/timing `93.766/94.267/0.5808s`, final invalid/non-monotonic/overlap `0/0/0`, final last end/duration bound `180.0/180.0`, short/long `0/0`, global max-active `1`, and timeout detected `false` at `output/manual_verification/latest/stt_worker_timeout_compare_nle_relink_preview_cache_nas_20260628/stt_worker_timeout_audit.md`. Jammini scout `.agents/sentinel/handoffs/20260628-083937-next-nle-taption-runtime-contract-scout.md` was accepted with scope narrowed away from project-storage relink schemas and UI flow changes. UI/layout behavior, subtitle quality policy, STT/default-cache policy, detector thresholds, persisted NLE disk-format, App Store packaging/signing/upload, DMG behavior, runtime undo/redo UI, and per-pixel NLE writes remain blocked/unchanged.
63. Direct SRT runtime NLE precedence was added for linked-project SRT open. `ui/editor/editor_project_open_native.py::open_project_segments_in_editor` now syncs runtime `NLEProjectState` from the exact direct SRT editor rows after project metadata merge, records `last_editor_sync_source=direct_srt_open`, and marks `direct_srt_precedence_contract=srt_timing_text_wins` so linked project or NLE-derived subtitle rows cannot overwrite directly opened SRT timing/text. `tests/test_project_segment_reload.py` and `tests/test_direct_srt_precedence_audit.py` prove editor rows, runtime NLE rows, and auxiliary project metadata stay consistent while persisted project storage remains clean of runtime NLE fields. Evidence: `output/manual_verification/latest/direct_srt_precedence_contract_20260628/direct_srt_precedence_contract.md`; editor/NLE text both use `latest direct SRT text`, editor/NLE timing both use `[1.2, 2.4]`, project metadata is restored only as auxiliary metadata, and storage clean is `true`. NAS HeyDealer current-head regression after the slice accepted at `output/manual_verification/latest/direct_srt_precedence_nas_heydealer_20260628/acceptance/reference_benchmark_acceptance.md` with elapsed `45.785s`, raw/final/reference `58/56/89`, quality/text/timing `93.766/94.267/0.5808s`, final invalid/non-monotonic/overlap `0/0/0`, final last end/duration bound `180.0/180.0`, short/long `0/0`, global max-active `1`, and timeout detected `false` at `output/manual_verification/latest/stt_worker_timeout_compare_direct_srt_precedence_nas_20260628/stt_worker_timeout_audit.md`. Jammini scout `.agents/sentinel/handoffs/20260628-090453-srt-precedence-contract-scout.md` was accepted for the direct SRT precedence contract. UI/layout behavior, subtitle quality policy, STT/default-cache policy, persisted NLE disk-format, App Store packaging/signing/upload, DMG behavior, runtime undo/redo UI, and per-pixel NLE writes remain unchanged.
64. NLE selection/view-state isolation evidence was added. `tests/test_nle_selection_view_state_isolation.py` now proves text selection, `TimelineCanvas.set_active` / `clear_active_visual`, and `TimelineWidget.set_active` update only view state while preserving canvas/global subtitle rows and avoiding project writes or runtime NLE operation journal appends. `tools/audit_nle_selection_view_state_isolation.py` statically checks `EditorSegmentsManualEditsMixin._on_selection_changed`, `_on_cursor_moved`, `TimelineCanvas.set_active`, `TimelineCanvas.clear_active_visual`, and `TimelineWidget.set_active` for forbidden validation/save/NLE write calls or subtitle-row assignments. Evidence: `output/manual_verification/latest/nle_selection_view_state_isolation_20260628/nle_selection_view_state_isolation.md`; audit `ready=true`, selection view-state-only contract `true`, model validation/project save/NLE writes allowed `false/false/false`, primary row rewrite allowed `false`, forbidden calls/assignments `0`, and runtime change applied `false`. NAS HeyDealer current-head regression after the slice accepted at `output/manual_verification/latest/nle_selection_view_state_nas_heydealer_20260628/acceptance/reference_benchmark_acceptance.md` with elapsed `45.999s`, raw/final/reference `58/56/89`, quality/text/timing `93.766/94.267/0.5808s`, final invalid/non-monotonic/overlap `0/0/0`, final last end/duration bound `180.0/180.0`, short/long `0/0`, global max-active `1`, STT1/STT2/word selected `21/37/7`, and timeout detected `false` at `output/manual_verification/latest/stt_worker_timeout_compare_nle_selection_view_state_nas_20260628/stt_worker_timeout_audit.md`. Jammini scout `.agents/sentinel/handoffs/20260628-092700-timeline-view-state-isolation-scout.md` was accepted for the view-state isolation direction. UI/layout behavior, subtitle quality policy, STT/default-cache policy, persisted NLE disk-format, App Store packaging/signing/upload, DMG behavior, runtime undo/redo UI, and per-pixel NLE writes remain unchanged.
65. NLE undo/redo runtime-state restore sync was added. `ui/project/project_session_runtime.py::sync_runtime_nle_state_from_editor_rows` builds a session-only runtime `NLEProjectState` from restored editor rows, and `ui/editor/undo_manager.py::UndoManager._restore` now calls it after cached segment restore with `last_editor_sync_source=undo_redo_restore`. `tests/test_editor_split_undo.py` proves split undo/redo restore NLE rows match visible restored captions and live STT preview rows stay outside the restored runtime NLE state; `tests/test_nle_undo_redo_runtime_state_audit.py` and `tools/audit_nle_undo_redo_runtime_state.py` prove operation journal count `0` and storage clean of `_nle_project_state`, `nle`, and `nle_snapshot`. Evidence: `output/manual_verification/latest/nle_undo_redo_runtime_state_20260628/nle_undo_redo_runtime_state.md`; audit `ready=true`, before signature `[("before", 30, 90)]`, after signature `[("after left", 30, 60), ("after right", 60, 90)]`, sync source `undo_redo_restore`, and runtime storage policy `object_attribute_only`. NAS HeyDealer current-head regression after the slice accepted at `output/manual_verification/latest/nle_undo_redo_runtime_state_nas_heydealer_20260628/acceptance/reference_benchmark_acceptance.md` with elapsed `45.497s`, raw/final/reference `58/56/89`, quality/text/timing `93.766/94.267/0.5808s`, final invalid/non-monotonic/overlap `0/0/0`, final last end/duration bound `180.0/180.0`, short/long `0/0`, global max-active `1`, STT1/STT2/word selected `21/37/7`, and timeout detected `false` at `output/manual_verification/latest/stt_worker_timeout_compare_nle_undo_redo_runtime_state_nas_20260628/stt_worker_timeout_audit.md`. Jammini scout `.agents/sentinel/handoffs/20260628-093842-next-nle-taption-runtime-contract-scout.md` was deferred as a future roughcut sidecar compatibility candidate; it was not adopted in this slice. UI/layout behavior, subtitle quality policy, STT/default-cache policy, persisted NLE disk-format, App Store packaging/signing/upload, DMG behavior, runtime undo/redo UI, and per-pixel NLE writes remain unchanged.
66. NLE smart split undo routing was corrected for Taption-style structural editing under text focus. `ui/editor/ux/editor_timeline_gap_split.py::_arm_gap_snapshot_undo_routing(...)` now accepts `allow_revision_drift`, and timeline smart split NLE-projection success arms snapshot undo routing with drift tolerance so `_route_undo()` restores the app snapshot instead of consuming the focused `QTextEdit` local undo stack after Qt cursor/revision updates. Evidence: `output/manual_verification/latest/nle_smart_split_undo_route_20260628/smart_split_undo_route.md`. Focused verification passed: smart split repro `1 passed`, full `tests/test_editor_split_undo.py` `3 passed`, and `tests/test_timeline_playhead_fit.py -k "smart_split or gap_generate or seg_to_gap"` `4 passed, 189 deselected`. NAS HeyDealer current-head regression after the slice accepted at `output/manual_verification/latest/nle_smart_split_undo_route_nas_heydealer_20260628/acceptance/reference_benchmark_acceptance.md` with elapsed `45.752s`, raw/final/reference `58/56/89`, quality/text/timing `93.766/94.267/0.5808s`, final invalid/non-monotonic/overlap `0/0/0`, final last end/duration bound `180.0/180.0`, short/long `0/0`, global max-active `1`, and timeout detected `false` at `output/manual_verification/latest/stt_worker_timeout_compare_nle_smart_split_undo_route_nas_20260628/stt_worker_timeout_audit.md`. Jammini scout `.agents/sentinel/handoffs/20260628-095359-smart-split-undo-route-scout.md` was accepted for the revision-drift owner path; broader UI, persisted NLE, STT/default, App Store, and per-pixel write scopes were deferred. UI layout/labels/colors/menus/popups, subtitle quality policy, STT/default-cache policy, persisted NLE disk-format, App Store packaging/signing/upload, DMG behavior, and per-pixel NLE writes remain unchanged.
67. NLE roughcut sidecar compatibility audit tooling was added. `tools/audit_nle_roughcut_sidecar_compat.py` builds a real roughcut sidecar fixture, writes `_render_plan.json` and `_edl.json`, verifies `load_stitched_cut_boundaries_for_srt_open(...)` restores stitched boundaries, runs NLE render/export parity across roughcut sidecar and exported-assets surfaces, and checks raw project storage remains clean of `_nle_project_state`, `nle`, and `nle_snapshot` even when those fields are injected before write. `tests/test_nle_roughcut_sidecar_compat_audit.py` proves the report is ready, final invalid/non-monotonic/overlap stays `0/0/0`, global max-active stays `1`, sidecar/storage forbidden key counts stay `0`, and persisted NLE fields remain blocked. Evidence: `output/manual_verification/latest/nle_roughcut_sidecar_compat_20260628/nle_roughcut_sidecar_compat.md`. Focused verification passed: py_compile for the new tool/test, audit tests `2 passed`, render/export/roughcut guard subset `12 passed, 9 deselected, 4 subtests passed`, and roughcut UI sidecar subset `3 passed, 35 deselected`. NAS HeyDealer current-head regression after the slice accepted at `output/manual_verification/latest/nle_roughcut_sidecar_compat_nas_heydealer_20260628/acceptance/reference_benchmark_acceptance.md` with elapsed `45.103s`, raw/final/reference `58/56/89`, quality/text/timing `93.766/94.267/0.5808s`, final invalid/non-monotonic/overlap `0/0/0`, final last end/duration bound `180.0/180.0`, short/long `0/0`, global max-active `1`, and timeout detected `false` at `output/manual_verification/latest/stt_worker_timeout_compare_nle_roughcut_sidecar_compat_nas_20260628/stt_worker_timeout_audit.md`. Jammini scout `.agents/sentinel/handoffs/20260628-100359-roughcut-sidecar-nle-compatibility-scout.md` was accepted with scope narrowed to compatibility proof. UI layout/labels/colors/menus/popups, subtitle quality policy, STT/default-cache policy, persisted NLE disk-format, App Store packaging/signing/upload, DMG behavior, and per-pixel NLE writes remain unchanged.
68. NLE selection sync validation was added. `core/project/nle_project_state.py` now exposes `nle_active_selection_signature(...)` and `assert_nle_active_selection_consistent(...)` so reload/restore checks can prove the visible active caption and runtime `NLEProjectState` active caption point at the same row, with exact segment-start priority at shared boundaries. `tools/audit_nle_selection_sync_validation.py` and `tests/test_nle_selection_sync_validation.py` prove reload-time editor rows, timeline active sec, runtime NLE rows, clean storage, and operation journal count `0`; evidence: `output/manual_verification/latest/nle_selection_sync_validation_20260628/nle_selection_sync_validation.md`. Focused verification passed: py_compile for the touched files, audit tests `3 passed`, reload guard subset `2 passed, 87 deselected`, and runtime NLE snapshot subset `2 passed, 13 deselected`. NAS HeyDealer current-head regression after the slice accepted at `output/manual_verification/latest/nle_selection_sync_validation_nas_heydealer_20260628/acceptance/reference_benchmark_acceptance.md` with elapsed `46.06s`, raw/final/reference `58/56/89`, quality/text/timing `93.766/94.267/0.5808s`, final invalid/non-monotonic/overlap `0/0/0`, final last end/duration bound `180.0/180.0`, short/long `0/0`, global max-active `1`, and timeout detected `false` at `output/manual_verification/latest/stt_worker_timeout_compare_nle_selection_sync_validation_nas_20260628/stt_worker_timeout_audit.md`. Jammini scout `.agents/sentinel/handoffs/20260628-101805-next-nle-taption-runtime-contract-scout.md` was accepted with scope narrowed away from UI redesign, persisted NLE fields, per-pixel writes, STT/default promotion, and App Store packaging. UI layout/labels/colors/menus/popups, subtitle quality policy, STT/default-cache policy, persisted NLE disk-format, App Store packaging/signing/upload, DMG behavior, and per-pixel NLE writes remain unchanged.
69. NLE relink parity verification was added. `core/project/nle_project_state.py` now exposes `nle_media_relink_parity_signature(...)` and `assert_nle_media_relink_parity(...)` so project-load/media relink checks can prove editor media files, runtime NLE snapshot clips/assets, runtime fps, and sequence duration agree after relink. `tools/audit_nle_relink_parity.py` and `tests/test_nle_relink_verification.py` prove successful relink parity, editor/timeline media path drift rejection, runtime fps drift rejection, operation journal count `0`, and clean persisted storage without `_nle_project_state`, `nle`, or `nle_snapshot`; evidence: `output/manual_verification/latest/nle_relink_parity_20260628/nle_relink_parity.md`. Focused verification passed: py_compile for the touched files, relink tests `5 passed`, NLE snapshot subset `4 passed, 11 deselected`, and selection/persistence guard `7 passed`. NAS HeyDealer current-head regression after the slice accepted at `output/manual_verification/latest/nle_relink_parity_nas_heydealer_20260628/acceptance/reference_benchmark_acceptance.md` with elapsed `48.693s`, raw/final/reference `58/56/89`, quality/text/timing `93.766/94.267/0.5808s`, final invalid/non-monotonic/overlap `0/0/0`, final last end/duration bound `180.0/180.0`, short/long `0/0`, global max-active `1`, STT1/STT2/word selected `21/37/7`, and timeout detected `false` at `output/manual_verification/latest/stt_worker_timeout_compare_nle_relink_parity_nas_20260628/stt_worker_timeout_audit.md`. Jammini scout `.agents/sentinel/handoffs/20260628-103348-next-nle-taption-runtime-contract-scout.md` was accepted with scope narrowed to validation helper/audit/test evidence. UI layout/labels/colors/menus/popups, subtitle quality policy, STT/default-cache policy, persisted NLE disk-format, project-storage relink schemas, automatic relink UI/dialog behavior, App Store packaging/signing/upload, DMG behavior, runtime undo/redo UI, and per-pixel NLE writes remain unchanged.
70. NLE final/preview isolation was added. `core/engine/subtitle_live_editor_feed.py` now exposes explicit `final_surface_segments` and `preview_lane_segments`, keeps `combined_segments` as diagnostic/candidate-lane only, and `ui/editor/editor_segments_timeline_context.py` keeps live STT/subtitle draft rows out of the video subtitle overlay while timeline/editor candidate lanes remain available. `tools/audit_nle_final_preview_isolation.py` proves feed final-surface isolation, preview-lane preservation, final overlay/global canvas filtering, and video overlay final-only behavior; evidence: `output/manual_verification/latest/nle_final_preview_isolation_20260628/nle_final_preview_isolation.md`. Focused verification passed: py_compile for touched files, `tests/test_subtitle_live_editor_feed_facade.py tests/test_project_segment_reload.py` `92 passed`, and NLE/timeline projection subset `18 passed, 185 deselected`. NAS HeyDealer current-head regression after the slice accepted at `output/manual_verification/latest/nle_final_preview_isolation_nas_heydealer_20260628/acceptance/reference_benchmark_acceptance.md` with elapsed `46.228s`, raw/final/reference `58/56/89`, quality/text/timing `93.766/94.267/0.5808s`, final invalid/non-monotonic/overlap `0/0/0`, final last end/duration bound `180.0/180.0`, short/long `0/0`, global max-active `1`, STT1/STT2/word selected `21/37/7`, and timeout detected `false` at `output/manual_verification/latest/stt_worker_timeout_compare_nle_final_preview_isolation_nas_20260628/stt_worker_timeout_audit.md`. Jammini scout `.agents/sentinel/handoffs/20260628-104950-next-nle-taption-runtime-contract-scout.md` was rejected/deferred because it duplicated the already completed playhead-jump isolation scope. UI layout/labels/colors/menus/popups, subtitle quality policy, STT/default-cache policy, persisted NLE disk-format, App Store packaging/signing/upload, DMG behavior, runtime undo/redo UI, and per-pixel NLE writes remain unchanged.
71. Taption voice-silence magnet parity was added for subtitle center body moves. `ui/editor/ux/timeline_subtitle_segment_editing.py` now reattaches local source metadata to native snap candidates, classifies silence-like `voice_activity`/`vad` rows as gap rows for center-drag gap-attachment suppression, and removes only those gap-like snap candidates while keeping real subtitle boundary candidates available beyond the silence row. Focused verification passed: py_compile for touched files; `tests/test_timeline_hit_targets.py -k "voice_silence or center_segment_move_prefers_subtitle_boundary_snap_beyond_gap or center_segment_move_suppresses_single_gap_snap_without_subtitle_target"` -> `4 passed`; `tests/test_timeline_hit_targets.py -k "gap or magnet or drag or boundary_release"` -> `58 passed`; `tests/test_timeline_playhead_fit.py -k "center_drag_over_single_gap_absorbs_gap_without_final_overlap or gap_generate or overwrite_trim"` -> `4 passed`. NAS HeyDealer current-head regression after the slice accepted at `output/manual_verification/latest/nle_voice_silence_magnet_nas_heydealer_20260628/acceptance/reference_benchmark_acceptance.md` with elapsed `348.171s`, raw/final/reference `55/57/89`, quality/text/timing `93.955/94.867/0.5536s`, final invalid/non-monotonic/overlap `0/0/0`, final last end/duration bound `180.0/180.0`, short/long `0/0`, and global max-active `1`. Timeout audit `output/manual_verification/latest/stt_worker_timeout_compare_nle_voice_silence_magnet_nas_20260628/stt_worker_timeout_audit.md` reports timeout detected `true` with STT1/Fast-STT2/word precision timeout/fallback, so this remains diagnostic evidence only and does not approve STT/default-cache policy changes. Jammini scout `.agents/sentinel/handoffs/20260628-110359-next-nle-taption-runtime-contract-scout.md` recommended neighbor collision validation; Dex deferred that adjacent scope and implemented the owner-requested silence/gap magnet gap instead. UI layout/labels/colors/menus/popups, subtitle quality policy, persisted NLE disk-format, App Store packaging/signing/upload, DMG behavior, runtime undo/redo UI, and per-pixel NLE writes remain unchanged.
72. NLE neighbor-collision guard validation was added for Taption-style subtitle segment edits at release/commit boundaries. `tests/test_project_nle_dual_write.py` now covers rejected center commit-row overlap and split-required resize collision without mutating project rows or creating runtime NLE state. `tools/audit_nle_neighbor_collision.py` and `tests/test_nle_neighbor_collision_audit.py` prove caption move overlap, caption move-commit overlap, split-required resize collision rejection, and partial resize neighbor trimming to a shared boundary. Evidence: `output/manual_verification/latest/nle_neighbor_collision_guard_20260628/nle_neighbor_collision_guard.md`; audit `ready=true`, check count `6`, final overlap gate `0`, and rejected collision mutation gate `true`. Focused verification passed: py_compile for touched files; `tests/test_nle_neighbor_collision_audit.py tests/test_project_nle_dual_write.py -k "neighbor_collision or overlap or center_overwrite_trim or split_required"` -> `9 passed, 33 deselected`; full owner suite `tests/test_project_nle_dual_write.py tests/test_nle_neighbor_collision_audit.py` -> `42 passed`; overlap/runtime cutover subset -> `11 passed, 5 deselected`. NAS HeyDealer current-head regression after the slice accepted at `output/manual_verification/latest/nle_neighbor_collision_nas_heydealer_20260628/acceptance/reference_benchmark_acceptance.md` with elapsed `94.953s`, raw/final/reference `58/56/89`, quality/text/timing `93.766/94.267/0.5808s`, final invalid/non-monotonic/overlap `0/0/0`, final last end/duration bound `180.0/180.0`, short/long `0/0`, global max-active `1`, STT1/STT2/word selected `21/37/7`, and timeout detected `false` at `output/manual_verification/latest/stt_worker_timeout_compare_nle_neighbor_collision_nas_20260628/stt_worker_timeout_audit.md`. Jammini scout `.agents/sentinel/handoffs/20260628-112236-neighbor-collision-validation-scout.md` was accepted with scope narrowed away from nonexistent `validate_segments`, UI redesign, persisted NLE fields, per-pixel writes, STT/default promotion, and App Store packaging.
73. Direct SRT and roughcut sidecar read-back parity was extended for owner-approved persisted `nle_snapshot` metadata without making the snapshot the load owner. `core/project/nle_snapshot.py` now supports surface-specific readback reports from exact editor rows, and `ui/editor/editor_project_open_native.py` records `surface=direct_srt_open` drift against directly imported SRT rows while preserving `srt_timing_text_wins`. `tools/audit_nle_persistence_cutover.py` now corrupts persisted roughcut-sidecar markers and proves the drift is detected while render/export and roughcut sidecar parity remain stable. Evidence: `output/manual_verification/latest/nle_direct_srt_roughcut_readback_parity_20260629_0003/nle_persistence_cutover_audit.md`; `prep_ready=true`, approved read-back stable `true`, corrupted marker drift detected `true`, render/export stable `true`, roughcut sidecar stable `true`, runtime report/runtime state/top-level `nle`/quarantine persisted `false/false/false/false`, operation roundtrip all passed across `11` families, and full cutover remains blocked. Focused verification passed: direct SRT subset `4 passed, 86 deselected`, NLE snapshot subset `4 passed, 12 deselected, 4 subtests passed`, and cutover audit `5 passed`.
74. Owner-approved top-level `nle` shadow metadata persistence was added without making NLE metadata the load owner. `core/project/nle_snapshot.py` builds `ai_subtitle_studio.nle_shadow_project.v1` payloads from the approved snapshot adapter, `core/project/project_format.py` writes them only when `nle_persistence.persist_snapshot=true`, `persist_top_level_nle=true`, and `approval=owner_approved_20260628`, and `core/project/nle_persistence_guard.py` allows only that shadow shape with an approved companion `nle_snapshot` while keeping `_nle_project_state` runtime-only. Evidence: `output/manual_verification/latest/nle_top_level_shadow_metadata_20260629_0020/nle_persistence_cutover_audit.md`; `prep_ready=true`, `top_level_nle_shadow_ready=true`, `canonical_load_owner=legacy_editor_state`, legacy rows stable `true`, read-back parity stable `true`, runtime report/runtime state/quarantine persisted `false/false/false`, operation roundtrip all passed across `11` families, render/export parity passed, and full cutover remains blocked by canonical-load and runtime-state gates. Focused verification passed: persistence guard `8 passed`, cutover audit `6 passed`, render/export parity `2 passed`, and NLE snapshot subset `16 passed, 4 subtests passed`.
