# App Store Owner-Input Package

This folder documents the owner-input package surface for Mac App Store
metadata. Generated package artifacts live under `output/manual_verification/latest/`.

Latest generated package:

- `output/manual_verification/latest/app_store_metadata_owner_input_package_v040116_20260629_0921/app_store_metadata_owner_input_package.md`

Current gate:

- `not_submission_proof=true`
- `owner_input_complete=false`
- `app_store_submission_ready=false`
- pending owner-input metadata `8/8`
- forbidden-claim scan `pass` with `0` matches

The package is a checklist and owner-input collection artifact only. It does
not replace Apple Distribution signing, installer package signing, sandbox
workflow smoke, App Store Connect validation, upload/submission, or
owner-approved metadata values.
