## ================================================================
## AI Subtitle Studio - Requirements (공통)
## Python 3.11
## 설치: python3.11 -m pip install -r requirements.txt
## ================================================================

# ================================
# UI / 기본 라이브러리
# ================================
PyQt6>=6.6.0
opencv-python>=4.9.0
Pillow>=10.0.0
numpy>=1.26.0
requests>=2.31.0
watchdog>=3.0.0
python-dotenv>=1.0.0
packaging>=23.0

# ================================
# Audio / Media I/O
# ================================
soundfile>=0.12.1

# ================================
# PyTorch (CPU 기본 — OS별 GPU 가속은 별도 파일 참조)
# ================================
torch
torchaudio

# ================================
# VAD (Voice Activity Detection)
# ================================
silero-vad>=6.2.1

# ================================
# 음성인식 (마이크 입력 / 인라인 편집)
# ================================
SpeechRecognition>=3.10.0
pyaudio>=0.2.14

# ================================
# LLM / AI API
# ================================
google-genai>=1.0.0
huggingface_hub>=0.20.0

# ================================
# Speaker Diarization (선택 / 실험적)
# ================================
# pyannote.audio는 torch/torchaudio 의존성 충돌 가능
# 필요 시 별도 환경에서 설치 권장
# pyannote.audio>=3.1.0