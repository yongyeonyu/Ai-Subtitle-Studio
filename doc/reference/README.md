# Reference Docs

This folder keeps guarded reference maps only. It is not a scratchpad.

## Keep

- `SUBTITLE_GENERATION_DOMAIN_MAP.md`: subtitle-domain owner map guarded by `tests/test_subtitle_generation_domain_map.py`.
- `LONG_FILE_OWNERSHIP_MAP.md`: long-file owner guard and split evidence guarded by `tests/test_subtitle_generation_domain_map.py`.

## Retired

- `File_structure.txt`: merged into `doc/README.md` and `doc/ARCHITECTURE.md`.
- `CODEMAP.md`: merged into `doc/ARCHITECTURE.md`, `doc/FEATURE_REGISTRY.md`, and the retained owner maps.
- `doc/DECISIONS/server_mode_benchmarking.md`: merged into `doc/ACTION_ITEMS.md` as a parked server-mode candidate.
- `doc/idea.md`: merged into `doc/ACTION_ITEMS.md` and `doc/HANDOFF.md`.

## Update Rule

Update these maps only when owner boundaries, long-file split status, or subtitle-domain guard tests change.
